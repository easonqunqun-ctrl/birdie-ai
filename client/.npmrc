# eslint-config-taro 把 eslint-plugin-import/react/react-hooks 等放在自己的 devDependencies，
# 而非 peerDependencies，pnpm 默认不提升这些间接依赖 → ESLint 找不到插件。
# 通过 public-hoist-pattern 把 eslint 生态提升到顶层 node_modules，不影响其他依赖的严格性。
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@typescript-eslint/*
