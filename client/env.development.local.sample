# 真机预览 / 体验版联调：复制为 `.env.development.local`（gitignore）后重建 watch：
#   cp env.development.local.sample .env.development.local
#   pnpm dev:weapp
#
# 域名须与微信公众平台「开发 → 开发管理 → 服务器域名 → request 合法域名」一致，且为 https。
TARO_APP_API_BASE_URL=https://api.birdieai.cn/v1
