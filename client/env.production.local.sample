# 本机发版：复制为 `.env.production.local`（gitignore，不打进仓库）
#   cp env.production.local.sample .env.production.local
# 将下方改为与公众平台 request 合法域名一致的 https://…/v1（可与线上 API 对齐）
TARO_APP_API_BASE_URL=https://api.birdieai.cn/v1
