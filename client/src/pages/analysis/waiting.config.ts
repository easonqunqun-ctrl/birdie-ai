export default definePageConfig({
  navigationBarTitleText: '分析中',
  navigationBarBackgroundColor: '#ffffff',
  navigationBarTextStyle: 'black',
  /** 下拉露出区域；与正文 `page { background: var(--color-primary) }` 一致（#1a237e） */
  backgroundColor: '#1a237e',
})
