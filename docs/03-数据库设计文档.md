# 「小鸟 AI」MVP 数据库设计文档

> 版本：v1.0  
> 日期：2026 年 4 月 14 日  
> 数据库：PostgreSQL 16  
> ORM：SQLAlchemy 2.x  
> 迁移工具：Alembic  
> 密级：内部机密

---

## 一、设计原则

1. **主键策略**：使用带前缀的 UUID 字符串（如 `usr_`, `ana_`, `chat_`），便于识别和调试
2. **时间字段**：所有时间使用 `TIMESTAMP WITH TIME ZONE`，存储 UTC 时间
3. **软删除**：核心业务表使用 `deleted_at` 字段实现软删除
4. **审计字段**：所有表包含 `created_at`、`updated_at` 字段
5. **JSON 字段**：复杂嵌套数据使用 `JSONB` 类型，并建立 GIN 索引
6. **枚举类型**：使用 PostgreSQL ENUM 类型或 VARCHAR + CHECK 约束

---

## 二、ER 关系图

```
users ─────────────┬──── swing_analyses ───── analysis_issues
  │                │           │
  │                │           └──── analysis_recommendations
  │                │
  ├──── analysis_quotas（月度分析配额）
  ├──── chat_quotas（每日对话配额）
  │                │
  │                │
  ├──── chat_sessions ──── chat_messages
  │
  ├──── training_plans ──── training_tasks ──── practice_logs
  │
  ├──── orders ──── payment_transactions
  │
  ├──── invitations
  │
  ├──── notifications
  │
  └──── feedback

drills（独立，训练动作库）
daily_tips（独立，每日小贴士库）
event_logs（独立，埋点数据）
```

---

## 三、表结构详细定义

### 3.1 users（用户表）

```sql
CREATE TABLE users (
    id              VARCHAR(32) PRIMARY KEY,            -- 格式: usr_<nanoid>
    wechat_openid   VARCHAR(64) NOT NULL UNIQUE,        -- 微信 OpenID
    wechat_unionid  VARCHAR(64),                        -- 微信 UnionID（可空）
    nickname        VARCHAR(48),                        -- 昵称，2-12 字符
    avatar_url      VARCHAR(512),                       -- 头像 URL
    
    -- 高尔夫档案
    golf_level              VARCHAR(20),                -- beginner/elementary/intermediate/advanced
    primary_goals           JSONB DEFAULT '[]',         -- ["distance","accuracy",...]
    weekly_practice_frequency VARCHAR(20),              -- occasional/once/frequent/daily
    onboarding_completed    BOOLEAN DEFAULT FALSE,
    has_completed_real_analysis BOOLEAN DEFAULT FALSE,  -- O-03：曾完成非示例分析，用于客户端隐藏示例入口
    
    -- 会员信息
    membership_type         VARCHAR(20) DEFAULT 'free', -- free/monthly/yearly/family
    membership_started_at   TIMESTAMPTZ,
    membership_expires_at   TIMESTAMPTZ,
    auto_renew              BOOLEAN DEFAULT FALSE,
    papay_contract_id     VARCHAR(64),              -- 微信委托代扣签约 contract_id（可空）
    
    -- 邀请
    invite_code             VARCHAR(8) NOT NULL UNIQUE, -- 6位邀请码
    invited_by_user_id      VARCHAR(32) REFERENCES users(id),
    
    -- 统计缓存（定期更新，减少实时聚合查询）
    total_analyses          INT DEFAULT 0,
    total_practices         INT DEFAULT 0,
    best_score              INT DEFAULT 0,
    current_streak_days     INT DEFAULT 0,
    max_streak_days         INT DEFAULT 0,
    last_practice_date      DATE,
    
    -- 元数据
    last_login_at           TIMESTAMPTZ,
    created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at              TIMESTAMPTZ,
    
    CONSTRAINT chk_golf_level CHECK (
        golf_level IN ('beginner', 'elementary', 'intermediate', 'advanced')
    ),
    CONSTRAINT chk_weekly_frequency CHECK (
        weekly_practice_frequency IN ('occasional', 'once', 'frequent', 'daily')
    ),
    CONSTRAINT chk_membership_type CHECK (
        membership_type IN ('free', 'monthly', 'yearly', 'family')
    )
);

-- 索引
CREATE INDEX idx_users_openid ON users(wechat_openid);
CREATE INDEX idx_users_invite_code ON users(invite_code);
CREATE INDEX idx_users_invited_by ON users(invited_by_user_id) WHERE invited_by_user_id IS NOT NULL;
CREATE INDEX idx_users_membership ON users(membership_type, membership_expires_at) WHERE membership_type != 'free';
CREATE INDEX idx_users_created_at ON users(created_at);
CREATE INDEX idx_users_deleted_at ON users(deleted_at) WHERE deleted_at IS NULL;
```

---

### 3.2 analysis_quotas（分析配额表 — 月度）

```sql
CREATE TABLE analysis_quotas (
    id                      VARCHAR(32) PRIMARY KEY,
    user_id                 VARCHAR(32) NOT NULL REFERENCES users(id),
    
    quota_month             VARCHAR(7) NOT NULL,          -- 格式: 2026-04
    used                    INT DEFAULT 0,                -- 已使用次数
    total                   INT DEFAULT 3,                -- 免费用户3次，会员-1（代表无限）
    bonus                   INT DEFAULT 0,                -- 邀请/分享获得的额外次数
    
    created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT uq_analysis_quota UNIQUE (user_id, quota_month)
);

CREATE INDEX idx_analysis_quotas_lookup ON analysis_quotas(user_id, quota_month);
```

**业务规则**：
- 每次请求分析时查找当月记录，不存在则自动创建
- 剩余次数 = `total + bonus - used`，`total = -1` 代表无限
- 会员过期时，当月已有记录的 total 保持不变（不回退已享受的权益），下个自然月开始恢复为 3
- 分析失败退回配额：`used -= 1`

---

### 3.3 chat_quotas（对话配额表 — 每日）

```sql
CREATE TABLE chat_quotas (
    id                      VARCHAR(32) PRIMARY KEY,
    user_id                 VARCHAR(32) NOT NULL REFERENCES users(id),
    
    quota_date              DATE NOT NULL,                -- 格式: 2026-04-14
    used                    INT DEFAULT 0,                -- 已使用轮次
    total                   INT DEFAULT 5,                -- 免费用户5次，会员-1（代表无限）
    
    created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT uq_chat_quota UNIQUE (user_id, quota_date)
);

CREATE INDEX idx_chat_quotas_lookup ON chat_quotas(user_id, quota_date);
```

**业务规则**：
- 每次发送对话时查找当日记录，不存在则自动创建
- 剩余次数 = `total - used`，`total = -1` 代表无限
- 每日 00:00（UTC+8）自然重置（通过创建新记录实现，无需定时任务）
- 旧的配额记录按 90 天策略清理

---

### 3.4 swing_analyses（挥杆分析表）

> **M2 实现偏差说明（2026-04 W3-W4 核查）**  
> 实际 alembic `0001_initial_schema.py` 的落库与下方 SQL 有以下差异，MVP 范围内**以迁移为准**：
>
> 1. **诊断与建议独立建表**（规范化，而非内嵌 JSONB）：
>    - `detected_issues JSONB` → 移到独立表 `analysis_issues`（见 §3.5）
>    - `recommendations JSONB` → 移到独立表 `analysis_recommendations`（drill_id / target_issue / sort_order 三列，详细步骤由前端 `client/src/constants/drillLibrary.ts` 查表）
> 2. **延后字段**：`feature_data JSONB`、`model_version`、`processing_started_at`、`processing_duration_ms`、`deleted_at` 暂未落库 —— 进度曲线/对比/软删除挂 **W7 会员能力** 再补迁移。
> 3. **新增字段**：`is_sample BOOLEAN DEFAULT FALSE`（**示例视频体验**，默认列表查询 `WHERE is_sample = FALSE`）—— 在 `0001_initial_schema.py` 中已建好，无需额外迁移。
> 4. **GIN 索引 `idx_analyses_issues`** 因 `detected_issues` 字段不存在而取消；查询 issues 走 `analysis_issues.analysis_id` 上的普通 B-Tree 索引。
> 5. **`quality_warnings`（`0015_swing_analysis_quality_warnings`）**：`JSONB`，存 AI 引擎返回的非阻断质量提示 machine codes 列表（如 `["low_light","camera_shake"]`），详见 [`docs/02` §3.4](./02-API接口设计文档.md)。

```sql
CREATE TABLE swing_analyses (
    id                  VARCHAR(32) PRIMARY KEY,         -- 格式: ana_<nanoid>
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    
    -- 输入
    video_url           VARCHAR(512) NOT NULL,           -- 原始视频 COS 路径
    video_duration      DECIMAL(5,2),                    -- 视频时长（秒）
    video_file_size     BIGINT,                          -- 文件大小（字节）
    camera_angle        VARCHAR(20) NOT NULL,            -- face_on / down_the_line
    club_type           VARCHAR(20) NOT NULL,            -- driver/iron_7/wedge/...
    
    -- 处理状态
    status              VARCHAR(20) NOT NULL DEFAULT 'pending',
    stage               VARCHAR(30),                     -- 当前处理阶段
    stage_progress      INT DEFAULT 0,                   -- 阶段进度 0-100
    error_code          INT,                             -- 失败错误码
    error_message       VARCHAR(500),                    -- 失败错误消息
    quota_refunded      BOOLEAN DEFAULT FALSE,           -- 是否已退回配额
    priority            VARCHAR(10) DEFAULT 'standard',  -- standard/priority
    
    -- 输出 - 评分
    overall_score       INT,                             -- 综合评分 0-100
    score_change        INT,                             -- 与上次的变化
    phase_scores        JSONB,                           -- 各阶段评分详情
    /*
    phase_scores 结构:
    {
      "setup": {"score": 82, "details": {...}},
      "backswing": {"score": 74, "details": {...}},
      "top": {"score": 80, "details": {...}},
      "downswing": {"score": 65, "details": {...}},
      "impact": {"score": 79, "details": {...}},
      "follow_through": {"score": 85, "details": {...}}
    }
    */
    
    -- 输出 - 诊断
    detected_issues     JSONB DEFAULT '[]',              -- 检测到的问题列表
    /*
    detected_issues 结构:
    [
      {
        "type": "casting",
        "name": "抛杆（Casting）",
        "severity": "high",
        "confidence": 0.92,
        "description": "...",
        "key_frame_url": "...",
        "key_frame_timestamp": 1.8,
        "metrics": {
          "wrist_release_angle": 35,
          "ideal_range": [15, 25]
        }
      }
    ]
    */
    
    -- 输出 - 建议
    recommendations     JSONB DEFAULT '[]',              -- 训练建议列表
    /*
    recommendations 结构:
    [
      {
        "drill_id": "drill_towel_arm",
        "reason": "修复 casting 问题",
        "priority": 1
      }
    ]
    */
    
    -- 输出 - 视觉资源
    skeleton_video_url  VARCHAR(512),                    -- 骨骼叠加视频 URL
    skeleton_data_url   VARCHAR(512),                    -- 骨骼数据 JSON 文件 URL
    share_card_url      VARCHAR(512),                    -- 分享卡片 URL
    thumbnail_url       VARCHAR(512),                    -- 缩略图 URL
    
    -- 输出 - 阶段时间戳
    phase_timestamps    JSONB,
    /*
    phase_timestamps 结构:
    {
      "setup": {"start": 0.0, "end": 0.8},
      "backswing": {"start": 0.8, "end": 1.5},
      ...
    }
    */
    
    -- 输出 - 原始特征数据（用于进步曲线和对比）
    feature_data        JSONB,
    /*
    feature_data 结构:
    {
      "spine_angle_address": 32.5,
      "spine_angle_impact": 30.1,
      "hip_rotation_backswing": 42.0,
      "hip_rotation_downswing": 55.0,
      "shoulder_rotation": 88.5,
      "x_factor": 46.5,
      "wrist_release_angle": 35.0,
      "swing_tempo_ratio": 2.8,
      "head_movement_cm": 3.2,
      ...
    }
    */
    
    -- 处理信息
    processing_started_at   TIMESTAMPTZ,
    processing_duration_ms  INT,                         -- 处理总耗时
    model_version           VARCHAR(20),                 -- 使用的模型版本
    
    analyzed_at         TIMESTAMPTZ,                     -- 分析完成时间
    is_sample           BOOLEAN NOT NULL DEFAULT FALSE,  -- 是否为示例视频体验（不计配额、不入列表，M2-T6）
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at          TIMESTAMPTZ,                     -- 用户侧软删除（可空；Alembic 0010）；列表/曲线筛「未删除」走部分索引 0011
    
    CONSTRAINT chk_camera_angle CHECK (camera_angle IN ('face_on', 'down_the_line')),
    CONSTRAINT chk_status CHECK (status IN ('pending', 'processing', 'completed', 'failed')),
    CONSTRAINT chk_priority CHECK (priority IN ('standard', 'priority')),
    CONSTRAINT chk_overall_score CHECK (overall_score IS NULL OR (overall_score >= 0 AND overall_score <= 100))
);

-- 索引
CREATE INDEX idx_analyses_user_id ON swing_analyses(user_id);
CREATE INDEX idx_analyses_user_date ON swing_analyses(user_id, created_at DESC);
CREATE INDEX idx_analyses_user_status ON swing_analyses(user_id, status);
CREATE INDEX idx_analyses_status ON swing_analyses(status) WHERE status IN ('pending', 'processing');
CREATE INDEX idx_analyses_created_at ON swing_analyses(created_at);
CREATE INDEX idx_analyses_club_type ON swing_analyses(user_id, club_type);
CREATE INDEX idx_analyses_deleted ON swing_analyses(deleted_at) WHERE deleted_at IS NULL;
CREATE INDEX idx_analyses_issues ON swing_analyses USING GIN (detected_issues);
```

---

### 3.5 chat_sessions（对话会话表）

```sql
CREATE TABLE chat_sessions (
    id                      VARCHAR(32) PRIMARY KEY,     -- 格式: chat_<nanoid>
    user_id                 VARCHAR(32) NOT NULL REFERENCES users(id),
    context_analysis_id     VARCHAR(32) REFERENCES swing_analyses(id),
    
    message_count           INT DEFAULT 0,
    last_message_at         TIMESTAMPTZ,
    last_message_preview    VARCHAR(200),                 -- 最后一条消息预览
    
    -- LLM 上下文管理
    system_prompt_version   VARCHAR(16),                  -- system prompt 版本（M3-T2 迁移 0002 加入）
    
    created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at              TIMESTAMPTZ
);

CREATE INDEX idx_chat_sessions_user ON chat_sessions(user_id, created_at DESC);
CREATE INDEX idx_chat_sessions_active ON chat_sessions(user_id, last_message_at DESC) WHERE deleted_at IS NULL;
```

**M3 实现偏差说明**：

- `system_prompt_version` 字段由迁移 `alembic/versions/0002_chat_system_prompt_version.py` 添加，**字段长度 `VARCHAR(16)`**（原 DDL 写的 10 太窄，无法容纳 `"v1-with-ctx"` 这类版本号；以迁移为准）
- 字段 **nullable**：T1 时期建的 session 没有这个字段，历史数据迁移后为 `NULL`，代码侧读取时按"未记录"处理，不影响 LLM 调用
- 每次**创建新会话**时后端按当前 `chat_prompt.SYSTEM_PROMPT_VERSION`（目前 `"v1"`）写入；会话生命周期内不再改动（即使 prompt 升级了，旧会话保持写入时的版本，方便后续 A/B 评估）

---

### 3.6 chat_messages（对话消息表）

```sql
CREATE TABLE chat_messages (
    id                  VARCHAR(32) PRIMARY KEY,         -- 格式: msg_<nanoid>
    session_id          VARCHAR(32) NOT NULL REFERENCES chat_sessions(id),
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    
    role                VARCHAR(10) NOT NULL,             -- user / assistant
    content             TEXT NOT NULL,
    
    -- 附件
    attachments         JSONB DEFAULT '[]',
    /*
    attachments 结构:
    [
      {"type": "image", "url": "..."},
      {"type": "drill_card", "drill_id": "drill_towel_arm", "name": "毛巾夹臂练习"},
      {"type": "video_card", "drill_id": "drill_towel_arm", "title": "毛巾夹臂练习示范"},
      {"type": "analysis_card", "analysis_id": "ana_def456", "score": 78}
    ]
    */
    
    -- LLM 元数据
    prompt_tokens       INT,
    completion_tokens   INT,
    model_name          VARCHAR(50),
    response_time_ms    INT,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT chk_role CHECK (role IN ('user', 'assistant'))
);

CREATE INDEX idx_messages_session ON chat_messages(session_id, created_at);
CREATE INDEX idx_messages_user ON chat_messages(user_id, created_at DESC);
```

---

### 3.7 training_plans（训练计划表）

```sql
CREATE TABLE training_plans (
    id                  VARCHAR(32) PRIMARY KEY,         -- 格式: plan_<nanoid>
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    
    week_start          DATE NOT NULL,                   -- 周一日期
    week_end            DATE NOT NULL,                   -- 周日日期
    
    source_analysis_id  VARCHAR(32) REFERENCES swing_analyses(id), -- 触发生成的分析
    ai_summary          TEXT,                            -- AI 周度总评
    
    total_tasks         INT DEFAULT 0,
    completed_tasks     INT DEFAULT 0,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT uq_user_week UNIQUE (user_id, week_start)
);

CREATE INDEX idx_plans_user_week ON training_plans(user_id, week_start DESC);
```

---

### 3.8 training_tasks（训练任务表）

```sql
CREATE TABLE training_tasks (
    id                      VARCHAR(32) PRIMARY KEY,     -- 格式: task_<nanoid>
    plan_id                 VARCHAR(32) NOT NULL REFERENCES training_plans(id),
    user_id                 VARCHAR(32) NOT NULL REFERENCES users(id),
    drill_id                VARCHAR(32) NOT NULL REFERENCES drills(id),
    
    scheduled_date          DATE NOT NULL,
    sort_order              INT DEFAULT 0,
    
    status                  VARCHAR(20) DEFAULT 'pending', -- pending/completed
    completed_at            TIMESTAMPTZ,
    verification_analysis_id VARCHAR(32) REFERENCES swing_analyses(id),
    
    created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT chk_task_status CHECK (status IN ('pending', 'completed'))
);

CREATE INDEX idx_tasks_plan ON training_tasks(plan_id);
CREATE INDEX idx_tasks_user_date ON training_tasks(user_id, scheduled_date);
CREATE INDEX idx_tasks_user_status ON training_tasks(user_id, status, scheduled_date);
```

---

### 3.9 practice_logs（练习日志表）

```sql
CREATE TABLE practice_logs (
    id                  VARCHAR(32) PRIMARY KEY,
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    task_id             VARCHAR(32) REFERENCES training_tasks(id),
    drill_id            VARCHAR(32) NOT NULL REFERENCES drills(id),
    
    practice_date       DATE NOT NULL,
    duration_minutes    INT,
    notes               TEXT,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_practice_user_date ON practice_logs(user_id, practice_date DESC);
CREATE INDEX idx_practice_user_month ON practice_logs(user_id, practice_date);
```

---

### 3.10 drills（训练动作库）

```sql
CREATE TABLE drills (
    id                  VARCHAR(32) PRIMARY KEY,         -- 格式: drill_<snake_case_name>
    name                VARCHAR(100) NOT NULL,
    name_en             VARCHAR(100),
    
    target_issues       JSONB NOT NULL DEFAULT '[]',     -- ["casting", "over_the_top"]
    description         TEXT NOT NULL,
    steps               JSONB NOT NULL DEFAULT '[]',     -- ["步骤1", "步骤2", ...]
    tips                JSONB DEFAULT '[]',              -- 注意事项
    
    duration_minutes    INT NOT NULL,
    sets                INT,
    difficulty          VARCHAR(10) NOT NULL,            -- easy/medium/hard
    
    illustration_url    VARCHAR(512),                    -- 示意图
    video_url           VARCHAR(512),                    -- 演示视频
    
    sort_order          INT DEFAULT 0,
    is_active           BOOLEAN DEFAULT TRUE,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT chk_drill_difficulty CHECK (difficulty IN ('easy', 'medium', 'hard'))
);

CREATE INDEX idx_drills_issues ON drills USING GIN (target_issues);
CREATE INDEX idx_drills_active ON drills(is_active) WHERE is_active = TRUE;
```

---

### 3.11 orders（订单表）

```sql
CREATE TABLE orders (
    id                      VARCHAR(32) PRIMARY KEY,     -- 格式: ord_<nanoid>
    user_id                 VARCHAR(32) NOT NULL REFERENCES users(id),
    
    plan_type               VARCHAR(20) NOT NULL,        -- monthly/yearly/family
    amount                  INT NOT NULL,                 -- 金额（分）
    currency                VARCHAR(3) DEFAULT 'CNY',
    
    status                  VARCHAR(20) NOT NULL DEFAULT 'pending',
    -- pending: 待支付, paid: 已支付, failed: 失败, refunded: 已退款, cancelled: 已取消
    
    -- 微信支付
    wechat_prepay_id        VARCHAR(128),
    wechat_transaction_id   VARCHAR(64),
    
    -- 会员期间
    membership_start        TIMESTAMPTZ,
    membership_end          TIMESTAMPTZ,
    
    paid_at                 TIMESTAMPTZ,
    refunded_at             TIMESTAMPTZ,
    refund_reason           VARCHAR(500),
    
    created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT chk_order_plan CHECK (plan_type IN ('monthly', 'yearly', 'family')),
    CONSTRAINT chk_order_status CHECK (status IN ('pending', 'paid', 'failed', 'refunded', 'cancelled'))
);

CREATE INDEX idx_orders_user ON orders(user_id, created_at DESC);
CREATE INDEX idx_orders_status ON orders(status) WHERE status = 'pending';
CREATE INDEX idx_orders_wechat ON orders(wechat_transaction_id) WHERE wechat_transaction_id IS NOT NULL;
```

---

### 3.12 payment_transactions（支付流水表）

```sql
CREATE TABLE payment_transactions (
    id                  VARCHAR(32) PRIMARY KEY,
    order_id            VARCHAR(32) NOT NULL REFERENCES orders(id),
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    
    transaction_type    VARCHAR(20) NOT NULL,             -- payment / refund
    amount              INT NOT NULL,                     -- 金额（分）
    
    -- 微信支付原始数据
    wechat_notify_data  JSONB,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_transactions_order ON payment_transactions(order_id);
CREATE INDEX idx_transactions_user ON payment_transactions(user_id, created_at DESC);
```

---

### 3.13 invitations（邀请记录表）

```sql
CREATE TABLE invitations (
    id                  VARCHAR(32) PRIMARY KEY,
    inviter_id          VARCHAR(32) NOT NULL REFERENCES users(id),
    invitee_id          VARCHAR(32) NOT NULL REFERENCES users(id),
    
    invite_code         VARCHAR(8) NOT NULL,
    status              VARCHAR(20) DEFAULT 'registered',
    -- registered: 仅注册, valid: 已完成首次分析（算有效邀请）
    
    -- 奖励
    inviter_bonus_type  VARCHAR(20),                     -- analysis / membership_days
    inviter_bonus_amount INT,
    bonus_granted       BOOLEAN DEFAULT FALSE,
    bonus_granted_at    TIMESTAMPTZ,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    CONSTRAINT uq_invitation UNIQUE (inviter_id, invitee_id),
    CONSTRAINT chk_invitation_status CHECK (status IN ('registered', 'valid'))
);

CREATE INDEX idx_invitations_inviter ON invitations(inviter_id, created_at DESC);
CREATE INDEX idx_invitations_invitee ON invitations(invitee_id);
```

---

### 3.14 share_actions（分享行为记录表）

```sql
CREATE TABLE share_actions (
    id                  VARCHAR(32) PRIMARY KEY,
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    
    share_type          VARCHAR(20) NOT NULL,             -- report / invite_poster / moments
    target_id           VARCHAR(32),                      -- 分享的报告 ID
    
    bonus_granted       BOOLEAN DEFAULT FALSE,
    bonus_type          VARCHAR(20),
    bonus_amount        INT,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_shares_user_date ON share_actions(user_id, created_at);
-- 用于查询今日是否已获得分享奖励
CREATE INDEX idx_shares_user_daily ON share_actions(user_id, (created_at::DATE)) WHERE bonus_granted = TRUE;
```

---

### 3.15 notifications（通知表）

```sql
CREATE TABLE notifications (
    id                  VARCHAR(32) PRIMARY KEY,
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    
    type                VARCHAR(30) NOT NULL,
    -- analysis_complete / training_reminder / membership_expiry / invite_reward / streak_achievement
    
    title               VARCHAR(100) NOT NULL,
    content             VARCHAR(500) NOT NULL,
    
    target_type         VARCHAR(20),                     -- analysis / training / membership
    target_id           VARCHAR(32),
    
    is_read             BOOLEAN DEFAULT FALSE,
    read_at             TIMESTAMPTZ,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_notifications_user ON notifications(user_id, created_at DESC);
CREATE INDEX idx_notifications_unread ON notifications(user_id, is_read) WHERE is_read = FALSE;
```

---

### 3.16 feedback（反馈表）

```sql
CREATE TABLE feedback (
    id                  VARCHAR(32) PRIMARY KEY,
    user_id             VARCHAR(32) NOT NULL REFERENCES users(id),
    
    content             TEXT NOT NULL,
    contact             VARCHAR(100),
    
    status              VARCHAR(20) DEFAULT 'pending',   -- pending / processing / resolved
    admin_notes         TEXT,
    
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_feedback_status ON feedback(status, created_at DESC);
```

---

### 3.17 daily_tips（每日小贴士表）

```sql
CREATE TABLE daily_tips (
    id                  VARCHAR(32) PRIMARY KEY,
    content             VARCHAR(500) NOT NULL,
    category            VARCHAR(20),                     -- technique / mindset / equipment / rule / fitness
    is_active           BOOLEAN DEFAULT TRUE,
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

### 3.18 event_logs（埋点事件表）

```sql
CREATE TABLE event_logs (
    id                  BIGSERIAL PRIMARY KEY,
    user_id             VARCHAR(32),
    
    event_name          VARCHAR(50) NOT NULL,
    event_params        JSONB DEFAULT '{}',
    
    device_info         JSONB,                           -- 设备信息
    ip_address          INET,
    
    event_time          TIMESTAMPTZ NOT NULL,
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 按时间分区（按月），提升查询性能
-- 生产环境建议使用 TimescaleDB 扩展或 PostgreSQL 原生分区

CREATE INDEX idx_events_user ON event_logs(user_id, event_time);
CREATE INDEX idx_events_name ON event_logs(event_name, event_time);
CREATE INDEX idx_events_time ON event_logs(event_time);
```

---

## 四、JSONB 字段结构规范

### 4.1 users.primary_goals

```json
["distance", "accuracy", "short_game", "putting", "consistency"]
```

允许的值枚举：`distance`, `accuracy`, `short_game`, `putting`, `consistency`

### 4.2 swing_analyses.phase_scores

```json
{
  "setup": {
    "score": 82,
    "details": {
      "stance_width": {"value": 78, "weight": 0.3},
      "knee_flex": {"value": 85, "weight": 0.25},
      "spine_tilt": {"value": 80, "weight": 0.25},
      "grip_position": {"value": 86, "weight": 0.2}
    }
  },
  "backswing": {
    "score": 74,
    "details": {
      "club_path": {"value": 70, "weight": 0.3},
      "left_arm_extension": {"value": 65, "weight": 0.25},
      "shoulder_hip_separation": {"value": 80, "weight": 0.25},
      "weight_transfer": {"value": 78, "weight": 0.2}
    }
  }
}
```

### 4.3 swing_analyses.detected_issues

```json
[
  {
    "type": "casting",
    "name": "抛杆（Casting）",
    "severity": "high",
    "confidence": 0.92,
    "description": "你的手腕在下杆初期就开始释放...",
    "key_frame_url": "https://cos.../frames/casting.jpg",
    "key_frame_timestamp": 1.8,
    "metrics": {
      "wrist_release_angle": 35,
      "ideal_range": [15, 25],
      "unit": "degrees"
    }
  }
]
```

### 4.4 chat_messages.attachments

```json
[
  {
    "type": "image",
    "url": "https://cos.../chat/img001.jpg",
    "width": 750,
    "height": 1000
  },
  {
    "type": "drill_card",
    "drill_id": "drill_towel_arm",
    "name": "毛巾夹臂练习",
    "description": "修复下杆时过早释放手腕",
    "duration_minutes": 15
  },
  {
    "type": "analysis_card",
    "analysis_id": "ana_def456",
    "overall_score": 78,
    "thumbnail_url": "https://cos.../thumb.jpg"
  }
]
```

---

## 五、迁移策略

### 5.1 Alembic 配置

```
项目目录/
├── alembic/
│   ├── versions/        # 迁移版本文件
│   ├── env.py
│   └── script.py.mako
├── alembic.ini
```

### 5.2 迁移规范

1. 每个迁移文件对应一个功能变更，命名格式：`YYYYMMDD_HHMM_<description>.py`
2. 所有迁移必须包含 `upgrade()` 和 `downgrade()` 方法
3. 生产环境迁移前需在测试环境验证
4. 不允许在迁移中删除列，只能标记废弃
5. 添加列时必须设置 DEFAULT 值或允许 NULL（避免锁表）
6. 大表添加索引使用 `CREATE INDEX CONCURRENTLY`

### 5.3 初始迁移顺序

```
001_create_users_table
002_create_analysis_quotas_table
003_create_chat_quotas_table
004_create_swing_analyses_table
005_create_chat_tables
006_create_training_tables
007_create_drills_table
008_create_orders_and_payments_tables
009_create_invitations_table
010_create_notifications_table
011_create_feedback_table
012_create_daily_tips_table
013_create_event_logs_table
014_seed_drills_data
015_seed_daily_tips_data
```

---

## 六、性能优化建议

### 6.1 读写分离

- 主库：处理写操作
- 只读副本：处理查询（分析历史列表、进步曲线、通知列表等）

### 6.2 缓存策略（Redis）

| 缓存键模式 | 缓存内容 | TTL |
|-----------|----------|-----|
| `user:{id}:info` | 用户基础信息 | 5 分钟 |
| `user:{id}:analysis_quota:{month}` | 当月分析配额 | 1 分钟 |
| `user:{id}:chat_quota:{date}` | 当日对话配额 | 1 分钟 |
| `analysis:{id}:status` | 分析任务状态 | 10 秒（轮询场景） |
| `home:{user_id}` | 首页聚合数据 | 5 分钟 |
| `drill:{id}` | 训练动作详情 | 1 小时 |
| `daily_tip:today` | 今日小贴士 | 24 小时 |
| `leaderboard:weekly` | 周榜单 | 10 分钟 |

### 6.3 数据归档

| 表 | 归档策略 |
|----|----------|
| event_logs | 90 天后归档至冷存储 |
| chat_messages | 180 天后归档 |
| swing_analyses（视频文件） | 免费用户 90 天后删除视频，保留分析数据 |

---

## 七、W7 实现偏差说明（2026-04）

§3.7-3.14 的表在 W7 真实落地时与设计稿存在以下小幅差异，记录方便后续 ALTER 或设计稿回修：

| 表 | 实现差异 | 原因 |
|---|---|---|
| `training_plans` | 加了 `(user_id, plan_week)` 唯一约束（设计稿未写） | 保证"一周一个计划"的业务不变式；增量分析时 upsert 而非插多条 |
| `training_tasks` | `steps JSONB` 字段在实现时是 drill 步骤的**快照**（而非引用 drill.id 到时重新查） | 避免后端改 `drills.steps` 时用户历史任务展示错乱 |
| `drills` | 加了 13 条种子数据（设计稿只说 3 条） | 与 `client/src/constants/drillLibrary.ts` 和 `ai_engine/app/mock_pipeline.py::DRILL_TEMPLATES` 三处对齐，Alembic `0004_training_system` 里 `bulk_insert` |
| `orders` | `prepay_id` 字段 W7 mock 模式下留空，只在真实支付下填 | Mock 时只有 `mock_confirm` 这一步，没有微信预下单流程 |
| `payment_transactions` | W7 每笔订单只写一条 `channel='mock'` 的流水 | 真实支付下会多一条 `channel='wechat_pay'`（W8） |
| `invitations` | 实现时 `status` 只有 `registered/valid` 两个值（设计稿一致） | W7 实装的 5-人档奖励靠"valid 数 ≥ 5 且未发过奖"判定，不需要 `rewarded` 状态；`bonus_granted` 字段已够用 |
| `share_actions` | W7 只用 `share_type='report'`；`bonus_*` 字段全留空 | 设计的"分享奖励每日上限 1 次"延后 W8；字段先占位 |

### 7.1 配额动态重算（W7-T1 新增行为）

`analysis_quotas` / `chat_quotas` 在 W7 之前是"创建时冻结 total"的，W7 在两处 hook 里**动态写 total**：

- **激活会员**（`payment_service.activate_membership` 在 `_mark_paid` 成功后）：
  把**当月** `analysis_quotas.total = -1`（无限）、**当日** `chat_quotas.total = -1`（无限）
- **过期降级**（`payment_service.ensure_membership_valid` 在每次 `get_current_user` 前懒检查）：
  若 `membership_expires_at <= now()` → `users.membership_type='free'` + `auto_renew=false`；同时把当月/当日配额中仍为 `-1` 的 `total` 回置为 `FREE_USER_MONTHLY_ANALYSES=3` / `FREE_USER_DAILY_CHATS=5`（`used` / `bonus` 不动）
- **新月/新日配额**（`quota_service.get_or_create_analysis_quota` / `get_or_create_chat_quota` 在首次消费时）：
  根据当前 `user.membership_type` 决定 `total` 的初始值（会员=`-1`、免费=`3` / `5`）

**关键取舍**：会员期内消费分析时，`quota.total<0` 的分支**不会**累加 `used`（见 `consume_analysis_quota`），所以会员期的消费不留"已用"痕迹；过期回落后，`used=0 + total=3 + bonus=0 → remaining=3`，得到一个干净的本月新额度。

`analysis_remaining(quota)` 的计算：若 `total<0` 直接返回 `9999`（前端识别为"无限"）；否则 `max(0, total + bonus - used)`。

### 7.2 迁移版本对照

| 迁移 | 里程碑 | 引入的表 |
|---|---|---|
| `0001_initial_schema` | M1-M3 | users / analysis_quotas / chat_quotas / swing_analyses / chat_sessions / chat_messages / feedback / notifications / event_logs |
| `0002_chat_system_prompt_version` | M3 | `chat_sessions` 增加 `system_prompt_version` 列 |
| `0003_orders_and_payments` | W7-T1 | orders / payment_transactions |
| `0004_training_system` | W7-T3 | training_plans / training_tasks / practice_logs / drills + 13 条 drill 种子 |
| `0005_invitations` | W7-T4 | invitations |
| `0006_share_actions` | W7-T5 | share_actions |

---

*文档完*
