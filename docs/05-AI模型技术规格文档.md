# 「小鸟 AI」MVP AI 模型技术规格文档

> 版本：v1.0  
> 日期：2026 年 4 月 14 日  
> 密级：内部机密

---

## 一、AI 能力总览

MVP 阶段需要实现的 AI 能力分为两大系统：

```
┌─────────────────────────────────────────────┐
│              AI 能力矩阵                      │
├─────────────────────┬───────────────────────┤
│   视觉分析系统       │   语言智能系统         │
├─────────────────────┼───────────────────────┤
│ ① 姿态估计          │ ⑥ AI 对话教练         │
│ ② 挥杆检测与分割    │ ⑦ 建议文案生成        │
│ ③ 特征提取          │ ⑧ 训练计划生成        │
│ ④ 动作评分          │                       │
│ ⑤ 问题诊断          │                       │
└─────────────────────┴───────────────────────┘
```

---

## 二、视觉分析系统

### 2.1 分析流水线架构

```
原始视频 → 预处理 → 姿态估计 → 挥杆检测 → 阶段分割
                                                │
    报告渲染 ← 建议生成 ← 问题诊断 ← 评分 ← 特征提取
```

**整体延迟目标**：端到端 < 30 秒（标准队列）/ < 15 秒（优先队列）

---

### 2.2 模块 ①：视频预处理

**输入**：用户上传的原始视频文件（MP4/MOV）

**处理步骤**：

| 步骤 | 操作 | 参数 | 说明 |
|------|------|------|------|
| 1 | 格式转码 | → MP4 (H.264) | 统一格式 |
| 2 | 帧率标准化 | → 30 fps | 统一时序基准 |
| 3 | 分辨率调整 | → 短边 720px，保持比例 | 平衡精度和速度 |
| 4 | 去抖动 | OpenCV stabilization | 减少相机抖动干扰 |
| 5 | 亮度/对比度归一化 | 自适应直方图均衡 | 适应不同光照 |
| 6 | 帧提取 | 全帧提取 + 关键帧标记 | 供后续模块使用 |

**输出**：

```python
class PreprocessResult:
    frames: list[np.ndarray]       # 所有帧 (H, W, 3)
    fps: float                      # 30.0
    total_frames: int
    duration_seconds: float
    resolution: tuple[int, int]     # (width, height)
    quality_score: float            # 0-1，视频质量评估
    quality_warnings: list[str]     # ["low_brightness", "high_shake"]
```

**工具选型**：OpenCV 4.x + FFmpeg

**性能目标**：10 秒视频预处理 < 3 秒

---

### 2.3 模块 ②：姿态估计

**模型选型**：

| 方案 | 精度 | 速度 | 选择 |
|------|------|------|------|
| MediaPipe Pose | 中高 | 快（CPU 可运行） | MVP 首选 |
| MoveNet Thunder | 高 | 中 | 备选方案 |
| MMPose (HRNet) | 很高 | 慢（需 GPU） | 后期升级 |
| 自训练模型 Fine-tune | 最高（高尔夫场景） | 中 | Phase 2 |

**MVP 方案**：MediaPipe Pose（33 个关键点）

**关键点定义（33 点）**：

```
0: nose                  11: left_shoulder      23: left_hip
1: left_eye_inner        12: right_shoulder     24: right_hip
2: left_eye              13: left_elbow         25: left_knee
3: left_eye_outer        14: right_elbow        26: right_knee
4: right_eye_inner       15: left_wrist         27: left_ankle
5: right_eye             16: right_wrist        28: right_ankle
6: right_eye_outer       17: left_pinky         29: left_heel
7: left_ear              18: right_pinky        30: right_heel
8: right_ear             19: left_index         31: left_foot_index
9: mouth_left            20: right_index        32: right_foot_index
10: mouth_right          21: left_thumb
                         22: right_thumb
```

**高尔夫场景重点关键点**（评分主要依赖）：

| 关键点 | 用途 |
|--------|------|
| 11, 12 (左右肩) | 肩部旋转角度 |
| 23, 24 (左右髋) | 髋部旋转角度 |
| 13, 14, 15, 16 (肘、腕) | 手臂角度、手腕释放 |
| 25, 26 (左右膝) | 膝盖弯曲度 |
| 0, 7, 8 (鼻、耳) | 头部位移 |
| 27, 28 (左右踝) | 重心转移 |

**输出**：

```python
class PoseResult:
    keypoints: np.ndarray           # (num_frames, 33, 3) — x, y, visibility
    confidence_scores: np.ndarray   # (num_frames,) — 每帧的整体置信度
    valid_frame_mask: np.ndarray    # (num_frames,) — bool，标记有效帧
```

**质量阈值**：

- 单帧置信度 < 0.5 → 标记为低质量帧
- 连续 > 30% 帧低质量 → 返回"视频质量不足"错误
- 人体关键点缺失 > 5 个 → 标记为不完整帧

**性能目标**：10 秒视频（300 帧）姿态估计 < 5 秒（GPU）/ < 10 秒（CPU）

---

### 2.4 模块 ③：挥杆检测与阶段分割

**挥杆检测**：

从连续帧中定位挥杆动作的起止点，丢弃非挥杆帧。

**检测方法**：

```
1. 计算手腕关键点（15, 16）的帧间位移速度
2. 速度 > 阈值的连续帧区间 = 候选挥杆区间
3. 选择最长 / 速度最大的候选区间为目标挥杆
4. 向前后各扩展 0.5 秒（包含准备和收杆）
```

**阶段分割**（6 阶段）：

| 阶段 | 英文 | 检测特征 |
|------|------|----------|
| 准备 | Setup / Address | 身体静止，双手在球前方 |
| 上杆 | Backswing | 手腕位置上升，肩部开始旋转 |
| 顶点 | Top | 手腕达到最高点，肩旋转最大 |
| 下杆 | Downswing | 手腕快速下降，髋部开始旋转 |
| 击球 | Impact | 手腕到达最低点附近 |
| 收杆 | Follow-through | 击球后手腕继续上升至收杆位 |

**分割算法**：

```
MVP 方案：基于规则 + 关键点速度/位置启发式

1. Top 点检测：手腕 Y 坐标（左手腕或右手腕，取决于正/反手）
   到达最高位的帧 = Top 帧
2. Impact 点检测：下杆阶段手腕速度最大 + Y 坐标最低的帧
3. Setup / Follow-through：挥杆区间的首/尾
4. Backswing = Setup 至 Top
5. Downswing = Top 至 Impact

后期方案：训练 1D 时序分类模型（LSTM / Transformer）
```

**输出**：

```python
class PhaseSegmentResult:
    phases: dict[str, PhaseInfo]
    # PhaseInfo: start_frame, end_frame, start_time, end_time, key_frame_idx

    swing_detected: bool
    swing_start_frame: int
    swing_end_frame: int
    handedness: str              # "right" / "left" 自动检测左右手
```

**精度目标**：

- 挥杆检测成功率 > 95%（有效视频）
- 阶段分割帧级误差 < ±3 帧（100ms @30fps）

---

### 2.5 模块 ④：特征提取

从骨骼关键点时序数据中提取高尔夫挥杆关键生物力学特征。

**特征清单（MVP 阶段 15 个核心特征）**：

| # | 特征名 | 计算方法 | 评分阶段 | 单位 |
|---|--------|----------|----------|------|
| 1 | 准备脊柱前倾角 | 肩中点-髋中点与垂直线的夹角 @Setup | Setup | 度 |
| 2 | 准备膝弯角 | 膝关节角度 @Setup | Setup | 度 |
| 3 | 肩旋转角（上杆） | 左右肩连线与目标线的夹角 @Top | Backswing | 度 |
| 4 | 髋旋转角（上杆） | 左右髋连线与目标线的夹角 @Top | Backswing | 度 |
| 5 | X-Factor | 肩旋转 - 髋旋转 @Top | Backswing | 度 |
| 6 | 左臂伸直度 | 左肘关节角度 @Top | Top | 度 |
| 7 | 顶点手腕位置 | 手腕相对于头部的位置 @Top | Top | 比例 |
| 8 | 下杆顺序指标 | 髋旋转先于肩旋转的时间差 @Downswing | Downswing | 帧数 |
| 9 | 手腕释放角 | 左手腕角度 @Impact vs @Top 的变化 | Downswing | 度 |
| 10 | 手腕释放时机 | 手腕角度开始变化的时间点（相对于下杆起点） | Downswing | 比例 |
| 11 | 击球脊柱前倾角 | 与准备位对比 | Impact | 度 |
| 12 | 头部位移量 | 头部（鼻尖）在整个挥杆过程中的水平位移 | 全程 | cm(估) |
| 13 | 挥杆节奏比 | 上杆时长 / 下杆时长 | 全程 | 比例 |
| 14 | 收杆高度 | 收杆时手腕位置高度 | Follow-through | 比例 |
| 15 | 收杆平衡 | 收杆结束后身体稳定性（关键点抖动量） | Follow-through | 像素 |

**角度计算方法**：

```python
def calculate_angle(point_a, point_b, point_c) -> float:
    """计算三点形成的角度（point_b 为顶点）"""
    ba = point_a - point_b
    bc = point_c - point_b
    cosine = np.dot(ba, bc) / (np.linalg.norm(ba) * np.linalg.norm(bc) + 1e-8)
    return np.degrees(np.arccos(np.clip(cosine, -1.0, 1.0)))

def calculate_rotation_angle(left_point, right_point, reference_line="horizontal") -> float:
    """计算两点连线与参考线的夹角（用于肩/髋旋转）"""
    diff = right_point - left_point
    angle = np.degrees(np.arctan2(diff[1], diff[0]))
    return angle
```

**输出**：

```python
class FeatureResult:
    features: dict[str, float]      # 特征名 → 特征值
    feature_metadata: dict[str, dict]
    # 每个特征的元信息：
    # {
    #   "value": 35.0,
    #   "frame_idx": 45,
    #   "confidence": 0.9,
    #   "unit": "degrees"
    # }
```

---

### 2.6 模块 ⑤：动作评分

**评分逻辑**：将用户特征值与职业球手参考范围对比，计算偏差得分。

**职业球手参考数据（初始值，持续校准）**：

| 特征 | 职业均值 | 理想范围 | 权重（在所属阶段内） |
|------|----------|----------|---------------------|
| 准备脊柱前倾角 | 30° | 25°-35° | 0.35 |
| 准备膝弯角 | 155° | 150°-165° | 0.30 |
| 肩旋转角（上杆） | 90° | 80°-100° | 0.35 |
| 髋旋转角（上杆） | 45° | 35°-55° | 0.25 |
| X-Factor | 45° | 35°-55° | 0.20 |
| 左臂伸直度 | 175° | 165°-180° | 0.50 |
| 顶点手腕位置 | 头顶上方 | — | 0.50 |
| 下杆顺序指标 | 髋先于肩 3-5 帧 | 2-7 帧 | 0.35 |
| 手腕释放角 | — | — | 0.30 |
| 手腕释放时机 | 下杆后 60% | 50%-70% | 0.35 |
| 击球脊柱前倾角 | 与准备位一致 | ±5° | 0.50 |
| 头部位移量 | < 5cm | 0-8cm | 0.50 |
| 挥杆节奏比 | 3:1 | 2.5:1 - 3.5:1 | 0.40 |
| 收杆高度 | 肩上方 | — | 0.50 |
| 收杆平衡 | < 阈值 | — | 0.50 |

**评分算法**：

```python
def score_feature(value: float, ideal_min: float, ideal_max: float,
                  tolerance: float = 0.5) -> int:
    """
    单特征评分（0-100）
    - 在理想范围内 → 85-100
    - 偏离理想范围但在容忍范围内 → 60-84
    - 偏离容忍范围 → 0-59
    """
    if ideal_min <= value <= ideal_max:
        center = (ideal_min + ideal_max) / 2
        range_half = (ideal_max - ideal_min) / 2
        deviation = abs(value - center) / range_half  # 0-1
        return int(100 - deviation * 15)  # 85-100

    if value < ideal_min:
        deviation = (ideal_min - value) / (ideal_max - ideal_min)
    else:
        deviation = (value - ideal_max) / (ideal_max - ideal_min)

    deviation = min(deviation / tolerance, 1.0)
    return int(84 - deviation * 84)  # 0-84

def score_phase(features: dict, phase_config: list) -> int:
    """阶段评分 = 加权特征评分之和"""
    total = sum(
        score_feature(features[f["name"]], f["ideal_min"], f["ideal_max"]) * f["weight"]
        for f in phase_config
        if f["name"] in features
    )
    return round(total)

def score_overall(phase_scores: dict) -> int:
    """综合评分 = 加权阶段评分之和"""
    weights = {
        "setup": 0.15, "backswing": 0.20, "top": 0.15,
        "downswing": 0.25, "impact": 0.15, "follow_through": 0.10
    }
    return round(sum(phase_scores[p] * w for p, w in weights.items()))
```

---

### 2.7 模块 ⑥：问题诊断

**MVP 覆盖 5 种常见错误模式**：

| # | 错误类型 | 英文 | 检测依据 | 严重度判定 |
|---|----------|------|----------|-----------|
| 1 | 抛杆 | Casting | 手腕释放时机 < 40%（过早） | 偏离越大越严重 |
| 2 | 由外到内下杆 | Over-the-top | 下杆初期手部轨迹方向（需 down_the_line 视角） | 偏离角度 |
| 3 | 提前伸展 | Early Extension | 击球时髋部前移量（脊柱角变化） | 变化量 |
| 4 | 侧移 | Sway / Slide | 上杆时髋部水平位移 > 阈值 | 位移量 |
| 5 | 失去身体姿势 | Loss of Posture | 挥杆过程中脊柱角度变化 > 阈值 | 变化量 |

**诊断算法（MVP：基于规则引擎）**：

```python
class IssueDiagnoser:
    def diagnose(self, features: FeatureResult, phases: PhaseSegmentResult) -> list[Issue]:
        issues = []

        # 1. Casting 检测
        release_timing = features["wrist_release_timing"]
        if release_timing < 0.40:
            severity = "high" if release_timing < 0.25 else "medium"
            issues.append(Issue(
                type="casting",
                name="抛杆（Casting）",
                severity=severity,
                confidence=min(1.0, (0.40 - release_timing) / 0.20),
                description=self._gen_casting_description(release_timing),
                metrics={"wrist_release_timing": release_timing, "ideal_range": [0.50, 0.70]}
            ))

        # 2. Early Extension 检测
        spine_change = abs(features["spine_angle_impact"] - features["spine_angle_setup"])
        if spine_change > 8:
            severity = "high" if spine_change > 15 else "medium" if spine_change > 10 else "low"
            issues.append(Issue(
                type="early_extension",
                ...
            ))

        # ... 类似逻辑处理其他错误类型

        # 按严重度排序
        severity_order = {"high": 0, "medium": 1, "low": 2}
        issues.sort(key=lambda x: severity_order[x.severity])

        return issues
```

**置信度**：每个诊断结果附带 confidence 值（0-1），< 0.6 的诊断不展示给用户（MVP 阶段保守策略）。

**后期升级路径**：规则引擎 → 训练 ML 分类器（使用标注数据） → 端到端深度学习模型

---

### 2.8 骨骼视频渲染

**功能**：在原始视频帧上叠加骨骼关键点和连线，生成可播放的分析视频。

**渲染规格**：

| 参数 | 值 |
|------|------|
| 关键点标记 | 圆形，半径 4px，绿色 |
| 骨骼连线 | 线宽 2px，白色，50% 透明 |
| 问题关键点高亮 | 红色闪烁标记 |
| 阶段标注 | 视频底部色条，标注当前阶段名称 |
| 角度标注 | 在关键角度位置显示角度数值 |
| 输出格式 | MP4 (H.264)，与原始分辨率一致 |
| 帧率 | 30fps |

**关键帧截图**：

- 每个诊断问题附带一张关键帧截图
- 截图上用红色圆圈/箭头标注出问题部位
- 截图格式：JPEG，质量 85%，宽 750px

---

## 三、语言智能系统

### 3.1 AI 对话教练

**架构**：

```
用户消息
   │
   ▼
┌──────────┐
│ 意图识别  │ ← 判断问题类型和所需知识领域
└────┬─────┘
     ▼
┌──────────┐     ┌────────────────┐
│ RAG 检索  │ ──→ │ 向量数据库      │
│          │ ←── │ (高尔夫知识库)   │
└────┬─────┘     └────────────────┘
     │
     ├── 检索到的知识片段
     ├── 用户历史分析摘要
     ├── 当前对话上下文
     │
     ▼
┌──────────┐
│ LLM 生成  │ ← System Prompt + Context
└────┬─────┘
     ▼
┌──────────┐
│ 后处理    │ ← 安全过滤 + 附件生成
└────┬─────┘
     ▼
  流式输出
```

**LLM 选型**：

| 模型 | 角色 | 说明 |
|------|------|------|
| DeepSeek V3 | 主力模型 | 中文能力强，性价比高 |
| Qwen2.5 72B | 备选模型 | 阿里云部署，延迟低 |

**调用参数**：

```python
LLM_CONFIG = {
    "model": "deepseek-chat",
    "temperature": 0.7,
    "max_tokens": 1024,
    "top_p": 0.9,
    "stream": True,
    "presence_penalty": 0.1,    # 轻微鼓励多样性
    "frequency_penalty": 0.1,
}
```

**System Prompt（核心）**：

```markdown
你是「小鸟 AI」的高尔夫教练，名字叫"小鸟教练"。你是一位经验丰富、耐心友善的高尔夫教学专家。

## 你的身份
- 你是 AI 高尔夫教练，专注于帮助中国高尔夫爱好者提升球技
- 你的语言风格：专业但不刻板，像一个耐心的教练朋友
- 你说中文，在必要时使用高尔夫英文术语（附带中文解释）

## 你的能力
- 分析用户的挥杆数据并给出改善建议
- 推荐针对性的练习方法
- 解答高尔夫技术、规则、装备等方面的问题
- 制定训练计划

## 你的限制
- 你不是医生，不提供伤病诊断和治疗建议。如果用户提到身体疼痛，建议就医
- 你不推荐具体品牌的球具（可以讲解选择标准）
- 你不回答与高尔夫无关的问题
- 你不知道其他用户的信息

## 回复要求
- 简洁明了，每次回复控制在 300 字以内
- 给出具体可执行的建议，而不是笼统的理论
- 如果引用用户的分析数据，明确说明数据来源
- 适当使用 emoji 让对话更生动（但不过度）

## 用户数据上下文
{user_analysis_context}
```

**用户数据上下文注入**：

```python
def build_user_context(user_id: str) -> str:
    analyses = get_recent_analyses(user_id, limit=3)
    if not analyses:
        return "该用户暂无挥杆分析数据。"

    context = "以下是该用户最近的挥杆分析数据：\n"
    for a in analyses:
        context += f"""
分析日期：{a.analyzed_at}
球杆：{a.club_type}
综合评分：{a.overall_score}分
各维度评分：{json.dumps(a.phase_scores, ensure_ascii=False)}
发现的问题：{json.dumps(a.detected_issues, ensure_ascii=False)}
"""
    return context
```

### 3.2 RAG 知识库

**知识库内容分类**：

| 类别 | 内容来源 | 预计条目数（MVP） |
|------|----------|-------------------|
| 挥杆技术 | 教学书籍摘要、教练视频文字稿 | 200+ |
| 练习方法 | 训练动作库文档 | 50+ |
| 常见问题 | 业余球手 FAQ | 100+ |
| 球场策略 | 通用球场策略（不含具体球场） | 50+ |
| 规则礼仪 | 高尔夫规则简化版 | 30+ |
| 装备知识 | 球杆选择指南、球的类型 | 30+ |

**文档处理流程**：

```
原始文档（PDF/Markdown/文本）
    │
    ▼
文本清洗 → 去除无关内容、统一格式
    │
    ▼
文本切片 → 按语义段落切分
    │       chunk_size: 500 字
    │       chunk_overlap: 50 字
    ▼
向量化 → Embedding 模型
    │     模型：text-embedding-3-small 或国产 Embedding
    │     维度：1536
    ▼
存入向量数据库 → Qdrant / Milvus
    │
    附带元数据：category, source, difficulty_level
```

**检索策略**：

```python
RAG_CONFIG = {
    "top_k": 5,                      # 检索 Top 5 相关片段
    "similarity_threshold": 0.7,     # 相似度阈值
    "rerank": True,                  # 是否进行重排序
    "rerank_model": "bge-reranker",  # 重排序模型
    "rerank_top_n": 3,               # 重排序后取 Top 3
}
```

### 3.3 建议文案生成

**用于生成分析报告中的问题描述和训练建议文案**。

**Prompt 模板**：

```markdown
请根据以下挥杆分析数据，为用户生成个性化的问题描述和训练建议。

用户信息：
- 打球经验：{golf_level}
- 主要目标：{primary_goals}

分析结果：
- 检测到的问题：{issues}
- 各维度评分：{phase_scores}
- 关键特征值：{features}

要求：
1. 问题描述用通俗易懂的中文，让非专业人士也能理解
2. 结合具体数据说明问题（如"你的手腕释放角度为 35°，理想值为 15-25°"）
3. 每个问题的描述控制在 2-3 句话
4. 训练建议要具体可执行
5. 按问题严重程度排序，最多输出 3 个主要问题
```

### 3.4 训练计划生成

**Prompt 模板**：

```markdown
请根据用户的挥杆分析结果和训练档案，生成本周训练计划。

用户信息：
- 每周练习频率：{weekly_practice_frequency}
- 打球水平：{golf_level}
- 主要改善目标：{primary_goals}

最新分析结果：
- 主要问题：{top_issues}
- 最弱维度：{weakest_phase}

可用训练动作库：
{available_drills}

要求：
1. 根据练习频率分配到具体日期
2. 优先针对最严重的问题安排训练
3. 每周 3-5 个训练任务
4. 由易到难安排顺序
5. 给出简短的周度总评/鼓励
6. 返回 JSON 格式
```

---

## 四、模型评估与质量保证

### 4.1 评估数据集

**MVP 阶段目标**：

| 数据集 | 规模 | 用途 |
|--------|------|------|
| 姿态估计验证集 | 100 段视频（人工标注关键点） | 验证 MediaPipe 在高尔夫场景的精度 |
| 阶段分割验证集 | 100 段视频（人工标注阶段帧号） | 验证分割精度 |
| 诊断验证集 | 200 段视频（教练标注问题类型） | 验证诊断准确率 |
| 评分校准集 | 50 段视频（教练打分） | 校准评分模型 |

### 4.2 评估指标

| 模块 | 指标 | MVP 目标 | 长期目标 |
|------|------|----------|----------|
| 姿态估计 | PCK@0.2（关键点正确率） | > 85% | > 95% |
| 挥杆检测 | 检测成功率 | > 95% | > 99% |
| 阶段分割 | 帧级 MAE | < 3 帧 | < 1 帧 |
| 问题诊断 | Precision（准确率） | > 70% | > 85% |
| 问题诊断 | Recall（召回率） | > 60% | > 80% |
| 评分 | 与教练评分 MAE | < 10 分 | < 5 分 |
| 评分 | 与教练评分相关系数 | > 0.7 | > 0.9 |

### 4.3 教练审核机制

**MVP 阶段**：
- 邀请 3-5 位职业教练作为顾问
- 每周抽样 20 份分析报告，教练人工审核
- 审核维度：诊断是否正确、建议是否合理、评分是否合理
- 审核结果回馈至模型迭代

**审核表**：

| 审核项 | 评分 | 说明 |
|--------|------|------|
| 诊断准确性 | 1-5 | 检测到的问题是否真实存在 |
| 诊断完整性 | 1-5 | 是否遗漏了明显问题 |
| 评分合理性 | 1-5 | 分数是否反映真实水平 |
| 建议有效性 | 1-5 | 建议的练习是否针对问题 |
| 文案质量 | 1-5 | 描述是否通俗易懂 |

### 4.4 模型版本管理

```
models/
├── pose_estimator/
│   ├── v1.0.0/               # MediaPipe 原始
│   └── v1.1.0/               # 高尔夫场景 fine-tune
├── phase_segmenter/
│   ├── v1.0.0/               # 规则引擎
│   └── v2.0.0/               # ML 模型
├── diagnoser/
│   ├── v1.0.0/               # 规则引擎
│   └── v2.0.0/               # ML 分类器
└── scorer/
    ├── v1.0.0/               # 参考范围对比
    └── v1.1.0/               # 校准后版本
```

**版本切换**：通过配置文件/环境变量指定各模块使用的模型版本，支持灰度发布（A/B 测试）。

---

## 五、降级与容错策略

| 场景 | 降级方案 |
|------|----------|
| 姿态估计失败 | 返回"视频分析失败"，退回配额 |
| 阶段分割失败 | 使用全视频分析（不分阶段），标注"仅供参考" |
| 诊断置信度全部 < 0.6 | 只展示评分，不展示具体问题，提示"当前视频角度/质量下难以精确诊断" |
| LLM 服务不可用 | 使用预置模板文案替代 LLM 生成 |
| RAG 检索无结果 | LLM 直接回答（不注入知识片段） |
| GPU 资源不足 | 将任务放入等待队列，增加预估等待时间 |

---

## 六、训练数据需求与采集计划

### 6.1 数据采集来源

| 来源 | 内容 | 预计量 | 获取方式 |
|------|------|--------|----------|
| 种子用户内测 | 真实用户挥杆视频 | 500+ | 用户授权 |
| 合作教练 | 教练拍摄的教学示范 | 100+ | 合作协议 |
| 公开数据集 | GolfDB 等学术数据集 | 1,400+ | 公开下载 |
| 自行录制 | 团队成员录制 | 50+ | 自制 |

### 6.2 标注规范

**标注工具**：CVAT / Label Studio

**标注内容**：

| 标注项 | 标注方式 | 标注人 |
|--------|----------|--------|
| 挥杆起止帧 | 帧级标注 | 初级标注员 |
| 6 阶段分割帧 | 帧级标注 | 初级标注员 |
| 错误类型 | 多标签分类 | 职业教练 |
| 错误严重度 | 三级分类 | 职业教练 |
| 整体评分 | 0-100 | 职业教练 |
| 各维度评分 | 0-100 × 6 | 职业教练 |

### 6.3 数据安全

- 所有标注数据脱敏处理（移除用户身份信息）
- 训练数据仅限内部使用
- 用户需明确授权视频用于模型训练（在隐私协议中声明）

---

## 七、算力资源需求

### 7.1 推理资源

| 组件 | 资源需求 | MVP 配置 |
|------|----------|----------|
| 姿态估计 | GPU（T4 即可） | 1× T4 GPU |
| 视频预处理 | CPU 密集 | 4 核 8G |
| 评分/诊断 | CPU 计算 | 与后端共用 |
| LLM 推理 | API 调用 | DeepSeek API |
| 向量检索 | 内存 + CPU | 2 核 4G |

### 7.2 并发能力估算

以 1× T4 GPU 为例：

| 场景 | 单任务耗时 | GPU 利用率 | 并发能力 |
|------|-----------|-----------|----------|
| 姿态估计（10 秒视频） | ~5 秒 | 80% | ~10 任务/分钟 |
| 完整分析流水线 | ~20 秒 | 40%（含 CPU 段） | ~15 任务/分钟 |

**MVP 初期预计峰值**：同时 10 个分析任务，单 GPU 可支撑。

### 7.3 成本估算

| 资源 | 月度成本（估） |
|------|---------------|
| T4 GPU 实例（按需） | ¥3,000-5,000 |
| DeepSeek API 调用 | ¥500-2,000 |
| Embedding API | ¥100-300 |
| 向量数据库实例 | ¥200-500 |
| **合计** | **¥3,800-7,800** |

---

*文档完*
