# 产品开发迭代计划 · 当前队列

> **维护约定**：与本仓库三份主线文档交叉引用并保持同步：
> - 功能与验收勾选：[docs/01-MVP功能需求规格说明书.md](./01-MVP功能需求规格说明书.md)
> - 上线能力与缺口对照：[docs/release-notes/W9-code-vs-plan-status.md](./release-notes/W9-code-vs-plan-status.md)
> - 并行工程泳道核销：[docs/release-notes/parallel-engineering-backlog.md](./release-notes/parallel-engineering-backlog.md)
> - **全量未闭环（合并排期）**：见本文 **§六**（汇总 `docs/01` 未勾 / 部分项、§二 §三、parallel backlog；**不替代** `docs/01` 原文）
> - **代码已交付、`docs/01` 未勾**：见 **§6.5b**（**DOC-\*** 文档勾选同步队列；与 §7.2 纪律一致）
> - **MVP 一期收尾（精简）**：见本文 **§七**（真引擎可观测、明确不做项；**详单仍以 §六 / `docs/01` 为准**）
> - **AI 引擎产品力（评分话术、ECS、三线迭代）**：见 [`docs/20-AI引擎产品力迭代设计.md`](./20-AI引擎产品力迭代设计.md)（白皮书 §5.2.1；**非**「发版前置一次性清单」，而是持续迭代真源）

---

## 一、优先级说明

| 标签 | 含义 |
|------|------|
| **P0** | 商业化 / 账务 / 生产运维：应在短迭代内落地或验收 |
| **P1** | 留存与合规体验：近两轮迭代内排期 |
| **P2** | 增长与引擎体验：按计划与资源并行 |
| **紧急队列** | 取自 W9 生产核销与上线 checklist、在**不发新功能**的前提下也必须闭环的项；与 P0 重叠时，**§二**为**可执行子集**（面向工程 / 运维的可核验动作） |

---

## 二、紧急队列（生产核销 / 发版前置）

下列项提炼自 [W9-code-vs-plan-status.md](./release-notes/W9-code-vs-plan-status.md)、[W9-launch-resource-checklist.md](./release-notes/W9-launch-resource-checklist.md)，并与 §三 队列矩阵中「代码已交付、生产仍须核销」的条目交叉引用。**ICP、小程序类目资质、密钥保管、备份策略** 等完整勾选表不在此重复；发版前须与 [W9-launch-resource-checklist.md](./release-notes/W9-launch-resource-checklist.md) **同步核销**。

| ID | 事项 | 验收动作（示例） | 锚点文档 / 路径 |
|----|------|------------------|-----------------|
| **U-1** | **Celery beat** 与待支付订单超时关闭 | 目标环境 `docker ps`（或等价编排面板）确认 **beat** 容器/进程持续运行；`docker logs`（或远端日志）确认 `expire_stale_pending_orders` / `xiaoniao.expire_stale_pending_orders` 按 `beat_schedule` 触发；核对 **`PAYMENT_PENDING_ORDER_EXPIRE_MINUTES`**（默认 `120`，`≤0` 关闭） | [`docker-compose.yml`](../docker-compose.yml) · [CVM-canonical-deploy.md](./release-notes/CVM-canonical-deploy.md) · [W9-code-vs-plan-status.md](./release-notes/W9-code-vs-plan-status.md) |
| **U-2** | **COS/CDN** 真桶与访问路径 | 除通过 [`test_storage_presign_contract.py`](../backend/tests/test_storage_presign_contract.py) 契约外，**人对桶**抽样：上传、读回、跨域；若启用 CDN，验证缓存与 HTTPS 行为 | [W9-code-vs-plan-status.md](./release-notes/W9-code-vs-plan-status.md) |
| **U-3** | **HTTPS** 与小程序**合法域名** | 真机侧避免 `request:fail errcode:-207`（须**公有 CA / Let's Encrypt** 等可信证书）；微信公众平台 **request / upload / socket** 域名与实控 API 主机一致，**勿带路径** `/v1` | [`infra/deploy/README.md`](../infra/deploy/README.md) · [W8-test-env-runbook.md](./release-notes/W8-test-env-runbook.md) · [W9-code-vs-plan-status.md](./release-notes/W9-code-vs-plan-status.md) |
| **U-4** | **支付 / 退款回调**域名与路径 | `WECHAT_PAY_NOTIFY_URL`、`WECHAT_PAY_REFUND_NOTIFY_URL`（或文档约定之等价配置）须 **HTTPS** 且与后端路由一致：`POST /v1/payments/wechat/notify`、`POST /v1/payments/wechat/refund-notify`（以 [docs/02-API接口设计文档.md](./02-API接口设计文档.md) §6.2 为准） | [W9-launch-resource-checklist.md](./release-notes/W9-launch-resource-checklist.md) · [docs/02-API接口设计文档.md](./02-API接口设计文档.md) |
| **U-5** | **部署形态**与 Runbook | 本地 Compose **≠** 生产：发版前将 Runbook、环境变量与实际控制栈对齐；排障 / 回滚口令见 CVM 文档 §0 | [CVM-canonical-deploy.md](./release-notes/CVM-canonical-deploy.md) · [W9-code-vs-plan-status.md](./release-notes/W9-code-vs-plan-status.md) |
| **U-6** | **叠加订单 / 按比例退款**（工单线） | 标准 **`apply-refund` + `refund-notify`** 已对齐微信 V3；多笔叠加续费等场景的 **`membership_end` 回拨**、按比例退仍以产品 / 法务工单迭代 | [docs/01-MVP功能需求规格说明书.md](./01-MVP功能需求规格说明书.md) §8 · §三 **Q-A1** |

---

## 三、队列矩阵（未完项）

| ID | P | 来源 | 事项 | 验收 / 锚点 |
|----|---|------|------|-------------|
| Q-A1 | P0 | W9 · code vs plan · M6 | **✅ `apply-refund` + `refund-notify`（非 mock）**；叠加单按比例退仍为工单；**紧急追踪**见 **U-6**；**工单字段模板**见 **§Q-A1 附** | §`docs/02` §6.2.1 / §6.2.2 |
| Q-A2 | P0 | W9 · 并行 backlog · CVM | **✅ Compose `celery-beat` 服务**；目标环境 beat **`docker ps`/`logs` 核验**见 **U-1** | [`docker-compose.yml`](../docker-compose.yml) · [`CVM-canonical-deploy.md`](release-notes/CVM-canonical-deploy.md) |
| Q-A3 | P1 | W9 · code vs plan | **COS/CDN**：契约测试外加「人对桶」抽样与 Runbook；**紧急追踪**见 **U-2** | [test_storage_presign_contract.py](../backend/tests/test_storage_presign_contract.py) |
| Q-B1 | P1 | docs/01 M2/M3 · M5 | **订阅消息**：前端 `requestSubscribeMessage`（waiting/membership，`subscribeTemplates.ts`）；**服务端** [`docs/02` §1.4.1](./02-API接口设计文档.md#141-微信小程序--分析完成订阅消息服务端非-rest)、**已过期** [§1.4.2](./02-API接口设计文档.md#142-微信小程序--会员到期惰性降级订阅消息服务端非-rest)、[`wechat_subscribe_message.py`](../backend/app/integrations/wechat_subscribe_message.py)。**仍规划**：**到期前**提醒（**O-02/O-14**）；对照 [§6.5](./19-产品开发迭代计划-当前队列.md#65-docs01-未勾--部分完成--plan-id对照表) | [waiting.tsx](../client/src/pages/analysis/waiting.tsx) · [membership.tsx](../client/src/pages/profile/membership.tsx) |
| Q-B2 | P2 | docs/01 §4 §6 | **并排历史报告对比**、海报 750×1334、`wxacode` | [W8-share-invite-roadmap](./release-notes/W8-share-invite-roadmap.md) |
| Q-B3 | P2 | docs/01 §5 · W8 | **进步曲线** UI：**会员** 综合分走势 + **practice_logs** 当月每日打卡柱状图；非会员锁定与引导（`training/index`）；多维折线 / 统计大卡仍为后续 | `training/index` · `GET /users/me/practice-logs` |
| Q-B4 | P2 | docs/01 §4 · W6 | 骨骼 FPS、质量门禁、`real_pipeline` 产品化 | [W6 roadmap](./release-notes/W6-engine-productization-roadmap.md) · [real_pipeline](../ai_engine/app/pipeline/real_pipeline.py) |
| Q-B5 | P2 | docs/01 §8 · W9 | **委托扣费 / 自动续费**（微信支付签约） | 依赖商户产品与模板 |
| Q-C1 | P2 | parallel backlog C1–C3 | 账号注销 / 会员过期 / 示例视频 §3.6 **文档逐项勾选与验收纪要** | [account_deletion](../client/src/pages/profile/account-deletion.tsx) · `ensure_membership_valid` |
| Q-D1 | P2 | W10 · RN | RN 里程碑 | [W10 milestones](./release-notes/W10-rn-milestones.md) · [18-W10](./18-W10-RN-App端规划.md) · [matrix](./release-notes/W10-weapp-apis-rn-matrix.md) |

### §Q-A1 附 · 叠加 / 按比例退款工单字段模板

> 范围：标准全额退款已在 [backend/app/api/v1/payments.py](../backend/app/api/v1/payments.py) 与 [wechat_pay_v3.py](../backend/app/integrations/wechat_pay_v3.py) 落地；本节为**产品 / 法务**侧仍需逐单评估的场景。U-6 紧急追踪点同条。
>
> 提单去 Lark / Jira 都行，字段沿用本表保持可对账；落库时映射到 `payment_transactions(type='refund')` 与 `orders.membership_end` 回拨。

#### 必填字段

| 字段 | 类型 / 取值 | 说明 / 验收规则 |
|------|-------------|------------------|
| `ticket_id` | str | 工单号；与 Lark/Jira 一致；进入 `payment_transactions.metadata.ticket_id` |
| `user_id` | int | 内部用户 ID（不暴露 openid） |
| `requested_at` | ISO8601 | 用户提单时间；用于 SLA 计算 |
| `orders[]` | list[Order] | 涉及订单列表；按 `paid_at` 升序排列，含「叠加续费」全部订单 |
| `orders[].order_id` | int | 必须存在 `orders` 表；`status ∈ {paid, refunded}` |
| `orders[].paid_amount_fen` | int | 实付金额（分）；与微信账单核对 |
| `orders[].membership_days` | int | 该订单实际贡献的会员天数（来自 `goods.duration_days`） |
| `orders[].consumed_days` | int | **已消耗**的天数（从 `membership_start` 起到工单生效日；按自然日向下取整） |
| `refund_strategy` | enum | `full` 全额退 / `prorated` 按比例退 / `denied` 不退 |
| `legal_basis` | str | 法务依据：用户协议第 X 条 / 平台风控 / 信用卡争议 等；**不可空**，留痕需要 |
| `membership_end_target` | ISO8601 \| null | 退款生效后 `users.membership_end` 应回拨到的目标日；`null` 表示完全降级（与全额退一致）|
| `refund_total_fen` | int | 实际退款总额（分）= Σ `orders[].refund_amount_fen` |
| `orders[].refund_amount_fen` | int | 按 `orders[].refund_strategy` 计算；公式见下 |
| `operator` | str | 客服 / 财务 / 法务负责人；至少一名 |
| `approved_by` | str | 二审签字人（按金额阈值约定）|

#### 按比例退款（`prorated`）金额公式

```
单笔退款额（分） = floor(paid_amount_fen × (membership_days - consumed_days) / membership_days)
```

- `consumed_days >= membership_days` → `refund_amount_fen = 0`（视同已消费完）
- 多笔叠加单：按 `paid_at` 倒序优先退「最晚生效那笔」剩余天数；**不要**跨订单合并按比例，避免与微信 V3 单笔退款的限额冲突
- 用户已有「赠送 / 邀请 / wxacode 兑换」获得的非购买天数：**不计入** `membership_days`，仅消耗顺序参与计算

#### `membership_end` 回拨

| 退款策略 | 回拨规则 |
|----------|----------|
| `full`（全额、单笔订单） | 由 `ensure_membership_valid` + `apply-refund` 标准链路自动降级（已实现）|
| `full`（全额、叠加单中最近一笔） | 回拨到 `membership_end - this_order.membership_days`；不要直接清零 |
| `prorated` | `membership_end_target` 必填，由人工核算后写入；脚本只校验「不晚于原 `membership_end`」 |
| `denied` | 不动 `membership_end`；仅在 `payment_transactions` 记 `refund_status=denied` |

#### 落库映射（仅人工执行时参考；当前**无**自动 API）

```sql
-- 1) 退款记账
INSERT INTO payment_transactions (order_id, type, amount_fen, status, metadata, created_at)
VALUES (:order_id, 'refund', :refund_amount_fen, 'succeeded',
        jsonb_build_object('ticket_id', :ticket_id, 'strategy', :strategy,
                           'legal_basis', :legal_basis, 'operator', :operator),
        now());

-- 2) 订单状态推进（按业务可设为 partially_refunded；MVP 阶段保留 refunded）
UPDATE orders SET status = 'refunded', refunded_at = now() WHERE id = :order_id;

-- 3) 会员到期回拨
UPDATE users SET membership_end = :membership_end_target WHERE id = :user_id;
```

落库前务必**手动**对照 [docs/03-数据库设计文档.md](./03-数据库设计文档.md) §6 的最新字段，部分字段可能已迁移到 `payment_transactions.metadata`。

#### 审计要点（法务）

- 工单审批链与退款金额一一对应；**金额阈值**与二审签字人由内部财务规章约定，本文不固定
- 真实凭据扣减：保留微信商户后台「退款详情」截图至 `docs/release-notes/refund-ticket-evidence/`（**不入库**，按团队规章自管）
- 用户告知：客服话术 + 短信 / 订阅消息（与 `wechat_subscribe_message.py` 模板对齐，待 Q-B1 落地）

---

## 四、已交付（对齐代码，避免误判为「未完」）

以下能力在源码中已实现，详见 [docs/01](./01-MVP功能需求规格说明书.md) / [parallel backlog](./release-notes/parallel-engineering-backlog.md) 的最近一次同步勾选。

- **会话列表**：`GET /chat/sessions` + [chat-history.tsx](../client/src/pages/profile/chat-history.tsx)
- **教练 / 训练 Tab 主链路**：[coach/index.tsx](../client/src/pages/coach/index.tsx) · [training/index.tsx](../client/src/pages/training/index.tsx)
- **上传层有限重试**：[analysisService.ts](../client/src/services/analysisService.ts)（E1）
- **微信支付**：JSAPI · 支付回调 · **退款申请 `/apply-refund` + `refund-notify`**：[wechat_pay_v3.py](../backend/app/integrations/wechat_pay_v3.py)、[payments.py](../backend/app/api/v1/payments.py)
- **Celery**：`expire_stale_pending_orders` 已由 **`docker-compose` `celery-beat`** 服务调度（见 [`docker-compose.yml`](../docker-compose.yml)）
- **分析完成订阅消息（O-06）**：`waiting` / `membership` 前端授权 + [`wechat_subscribe_message.py`](../backend/app/integrations/wechat_subscribe_message.py) + `analysis_tasks` 完成后下发（[`docs/02` §1.4.1](./02-API接口设计文档.md#141-微信小程序--分析完成订阅消息服务端非-rest)）
- **会员已过期订阅消息（O-02/O-14 子集）**：`ensure_membership_valid` → §1.4.2 惰性下发（**到期前 N 天**仍开放）

---

## 五、运维清单（占位）

上线资源逐项勾选见：[W9-launch-resource-checklist.md](./release-notes/W9-launch-resource-checklist.md)（与部署 Runbook 配合使用）。

---

## 六、全量未闭环开发计划（合并清单）

本节将 **待发版核销**、**队列矩阵**、[`docs/01`](./01-MVP功能需求规格说明书.md) 中全部 **`- [ ]` / `- [~]`**、以及 [`parallel-engineering-backlog.md`](./release-notes/parallel-engineering-backlog.md) 的 **Open / Partial** 汇入**一张主表**（§6.3），并通过 **§6.5 对照表**与 `docs/01` 验收视图逐行对应；§6.6 为推荐批次；§6.7 为执行纪律摘录。便于站会引用 **PLAN-ID** 排期。

**范围**：列入计划 **≠** 单一迭代内全部交付；**建议阶段**仅表达依赖与优先级批次。

### 6.1 统计摘要

| 来源 | 计数 | 说明 |
|------|------|------|
| `docs/01` **`- [ ]`** | **9** | 未勾选验收行（以 `docs/01` 为准；**O-03** 已回填 2026-05-18） |
| `docs/01` **`- [~]`** | **6** | 部分完成 / 余量（到期提醒 7/1 天、进步曲线余量、**O-10** 硬预检等） |
| **§6.5b DOC-\*** | **2** | 文档勾选同步队列中仍 **Open**（验收纪要类） |
| 本文 **§二 U-1～U-6** | 6 | 生产核销（与部分 Q 行重叠，主表互链） |
| 本文 **§三 Q-A1～Q-D1** | 10 | 产品/工程队列 |
| parallel **Open 3 + Partial 2** | 5 | 各独立一行（与 Q-C1 / 工程债可交叉） |

同一主题可能在 **U、Q、`docs/01`** 多处出现：**以 Q/U 为工程锚点**，本表 **依赖/备注** 列互指，避免重复描述分歧。

### 6.2 建议阶段说明

| 阶段 | 含义 |
|------|------|
| **A** | 发版前核销（运维 / 真桶 / HTTPS / Runbook，与 §二 一致） |
| **B** | P1 短期：订阅消息服务端、提醒类闭环 |
| **C** | P2：分享增长、海报、wxacode、进步曲线余量、委托扣费等 |
| **D** | W6：真实引擎、骨骼 FPS、质量预检与门禁 |
| **E** | W10：RN 里程碑 |
| **F** | 合规与文档验收（账号注销/会员/示例等 **纪要勾选**） |
| **G** | 工程健康（Toast 映射、pytest 覆盖面等） |

### 6.3 主表（PLAN-ID）

| PLAN-ID | 类型 | 建议阶段 | 来源 | 事项摘要 | 依赖或备注 |
|---------|------|----------|------|----------|------------|
| **U-1** | 核销 | A | §二 | Celery **beat** + `expire_stale_pending_orders` 目标环境核验 | 同 **Q-A2** |
| **U-2** | 核销 | A | §二 | **COS/CDN** 人对桶抽样、Runbook | 同 **Q-A3** |
| **U-3** | 核销 | A | §二 | **HTTPS**、小程序合法域名（含 `-207` 规避） | |
| **U-4** | 核销 | A | §二 | 支付/退款回调域名与路径对齐 `docs/02` §6.2 | |
| **U-5** | 核销 | A | §二 | 部署形态与 Runbook、实控环境对齐 | |
| **U-6** | 工单 | A/C | §二 · **Q-A1** | 叠加单 / **按比例退款**、会员截止回拨等产品-法务工单 | 标准退款已代码落地 |
| **Q-A1** | 工单 | A/C | §三 | 退款主路径已 ✅；余量见 **U-6** | |
| **Q-A2** | 核销 | A | §三 | Beat **生产侧**核验 | 见 **U-1** |
| **Q-A3** | 核销 | A | §三 | COS 契约外真桶验收 | 见 **U-2** |
| **Q-B1** | 功能 | B | §三 | 前端已 `requestSubscribeMessage`；服务端 §1.4.1 / §1.4.2 / §1.4.3、`wechat_subscribe_message.py` | **已交付**：分析完成、**已过期**、**到期前 N 天（默认 3）** Celery + Redis 去重（须模板 ID + beat）。**余量**：§3.5 的 **7/1 天** 多档与站内弹窗 |
| **Q-B2** | 功能 | C | §三 | 并排历史报告、海报 750×1334、`wxacode` | [W8-share-invite-roadmap](./release-notes/W8-share-invite-roadmap.md) |
| **Q-B3** | 功能 | C | §三 | 进步曲线 **余量**：多维折线、统计大卡等（MVP 柱状+得分已上线） | `training/index` |
| **Q-B4** | 功能 | D | §三 | 骨骼 FPS、质量门禁、`real_pipeline` 产品化 | [W6 roadmap](./release-notes/W6-engine-productization-roadmap.md) |
| **Q-B5** | 功能 | C | §三 | **委托扣费 / 自动续费**（微信支付签约） | 商户签约与模板 |
| **Q-C1** | 文档验收 | F | §三 · parallel | 账号注销 / 会员过期 / 示例 §3.6 **文档逐项勾选与验收纪要** | 亦见 **par-C1～C3** |
| **Q-D1** | 功能 | E | §三 | **RN** 里程碑 | [W10](./18-W10-RN-App端规划.md) |
| **O-01** | 功能 | B | docs/01 §3.1 | 新用户首次登录全流程 **&lt; 3 秒**（真机性能验收） | |
| **O-02** | 功能 | B | docs/01 §3.5 | **到期提醒**按时发送（订阅消息 / 站内运营） | **已部分交付**：会员过期经 `ensure_membership_valid` 后 **`send_membership_expired_notification`**（§1.4.2）。**仍缺**：**到期前**定时提醒与 `docs/01`「按时」全文验收对齐；同 **O-14**、**Q-B1** |
| **O-03** | 功能 | F | docs/01 §3.6 | 首次正式分析后 **隐藏**示例入口（当前常显） | |
| **O-04** | 核销 | C | docs/01 §4.2 | **100MB** 视频 4G **&lt; 60s** 真机实测 | W8 发布准备 |
| **O-05** | 决策 | C | docs/01 §4.2 | **断点续传** — MVP **明确不做**，后续评估 | |
| **O-06** | 功能 | B | docs/01 §4.2 | 离开等待页仍可收到 **完成通知**（`subscribeMessage.send` 等） | **已交付**（可配置）：分析完成 **§1.4.1**、`analysis_tasks` 提交后下发；与 **Q-B1**。`docs/01` 脚注见本文 **§6.5** |
| **O-07** | 功能 | D | docs/01 §4.3 | 骨骼叠加 **≥24fps** | W6 |
| **O-08** | 功能 | D | docs/01 §4.3 | 质量检测 **5s** 内完成（真实预检） | W6 |
| **O-09** | 功能 | D | docs/01 §4.3 | 阻断性问题 **100%** 拦截 | W6 |
| **O-10** | 功能 | D | docs/01 §4.3 | 警告性不阻断、报告中注明受影响 | W6 |
| **O-11** | 功能 | C | docs/01 §7.1 | 分享 **750×1334 PNG** 海报 | 与 **Q-B2** |
| **O-12** | 功能 | C | docs/01 §7.1 | **小程序码**扫码入小程序 | 与 **Q-B2** |
| **O-13** | 功能 | C | docs/01 §8.4 | **自动续费**开通/关闭 | 与 **Q-B5** |
| **O-14** | 功能 | B | docs/01 §8.4 | **到期提醒通知**准时 | **已部分交付**：**已过期**场景见 §1.4.2。**仍缺**：**到期前**准时、与模板审核及用户授权次数策略；与 **O-02** |
| **P-01** | 功能 | C | docs/01 §4.3 | 「对比历史」并排：选 2 条 → 六维/问题摘要对比页 | **已交付**：`compare.tsx` + `history?mode=compare` |
| **P-02** | 余量 | C | docs/01 §6.2 | 非高尔夫问题识别与引导：**定量测评**待 W8 | |
| **P-03** | 余量 | C | docs/01 §6.3 | 进步曲线：全量历史点、多维折线、统计大卡 | 与 **Q-B3** |
| **P-04** | 余量 | A/C | docs/01 §6.3 | 免费用户进步曲线引导：已有限定卡；**抛光**与支付开关策略 | |
| **par-C1** | 文档验收 | F | parallel | 账号注销全流程：`docs/01` §3.4 四项逐条勾选 + 纪要 | 汇总 **Q-C1** |
| **par-C2** | 文档验收 | F | parallel | 会员过期与降级：`docs/01` §3.5 与 `ensure_membership_valid` 对照 | 汇总 **Q-C1** |
| **par-C3** | 文档验收 | F | parallel | 示例视频 §3.6 与 M2 示例条目 **文档对齐** | 汇总 **Q-C1**；与 **O-03** |
| **par-E3** | 工程健康 | G | parallel | 网络 Toast 与 `request.ts` **error 映射**持续补全 | Partial |
| **par-T1** | 工程健康 | G | parallel | 后端 **pytest** 关键路径扩展 | Partial |
| **ENG-01** | 功能 | B/D | `docs/20` · docs/01 **§4.5** | 报告页「**分数说明**」入口 + 帮助/FAQ 链（文案 `docs/20` §4.2～§4.3） | **已交付**：`pages/help/score-guide`；与 **Q-B4** 同迭主要为体验抛光 |
| **ENG-02** | 功能 | D | `docs/20` · docs/01 **§4.5** | 报告页 **质量警告 / 低置信** 与重拍/机位建议（`quality_warnings` 全链路） | **已交付**：`ai_engine` → DB → `GET /analyses/{id}` & `/public` → `report.tsx`「拍摄提示」+ [`qualityWarnings.ts`](../client/src/constants/qualityWarnings.ts)。**仍与 §4.3**：**O-08** 预检数据源、**O-10**「警告不阻断」全文验收对齐 W6 |
| **ENG-03** | 文档/运营 | F | `docs/20` · docs/01 **§4.5** | 《评分与异议话术》**一页纸**（四类模板） | **已交付**：[`docs/09` §八](./09-运营支撑文档.md#八客服评分与异议话术eng-03)；运营执行与帮助页同步维护 |
| **ENG-04** | 功能 | D | `docs/20` · docs/01 **§4.5** | **ECS v1** + 引擎发版 **回归快照/漂移门禁** | **部分**：[`ecs-v1-starter-checklist`](./release-notes/ecs-v1-starter-checklist.md) 启动清单 ✅；**样本满编 + CI 回归快照**仍 **D** 与 **Q-B4** |
| **ENG-05** | 功能 | C/D | `docs/20` · docs/01 **§4.5** | 评分 **纵向/分位** 主叙事（接口变更先改 `docs/02`） | **部分（MVP）**：`score_change` 旁「较最近一次同类型分析」✅；**分位 / 进步曲线全量**与 **P-03** / **Q-B3** |
| **ENG-06** | 流程 | F | `docs/20` · docs/01 **§4.5** | **争议样本周更**复盘 + 纪要模板 | **部分**：[`docs/09` §8.5](./09-运营支撑文档.md#85-争议周更纪要模板eng-06) 模板 ✅；**例会节奏与执行**仍属运营纪律 |
| **DOC-01** | 文档 | F | docs/01 回填 | **O-06**：§4.2「离开等待页仍收到完成通知」与代码对齐 | **Done（2026-05-18）**：`docs/01` 已 `[x]`；见 §四订阅消息 |
| **DOC-02** | 文档 | F | docs/01 回填 | **O-02/O-14**：§3.5 + §8.4「到期提醒」区分 **已过期 ✅** / **到期前 ⏳** | **Done（2026-05-18）**：`docs/01` 已 `[~]`；功能余量仍跟 **Q-B1** |
| **DOC-03** | 文档 | F | docs/01 回填 | **O-10** + **ENG-02**：§4.4 软警告与 `quality_warnings` 对齐 | **Done（2026-05-18）**：`docs/01` 已 `[~]`；硬预检仍 **O-08** / **Q-B4** |
| **DOC-04** | 文档 | F | parallel **par-C1～C3** · **Q-C1** | 账号注销 / 会员过期 / 示例 §3.6：**验收纪要**逐项勾选（源码已 `[x]`） | **Open**：见 [`parallel-engineering-backlog.md`](./release-notes/parallel-engineering-backlog.md) C1–C3 |
| **DOC-05** | 文档 | F | `docs/01` 巡检 | 季度扫描「脚注写已实现、主行仍 `- [ ]`」→ 归入 **DOC-\*** 或当场回填 | **纪律**：发版前与 **§7.2** 同检；避免 §6.1 backlog 虚高 |

### 6.5 `docs/01` 未勾 / 部分完成 ↔ PLAN-ID（对照表）

下列与 [`docs/01-MVP功能需求规格说明书.md`](./01-MVP功能需求规格说明书.md) 中仍为 **`- [ ]` / `- [~]`** 的验收行对应（**§4.5 已全部 `[x]`**，改见 **§6.5a**）。便于站会只记 **PLAN-ID**。**不替代** `docs/01` 原文勾选。

| docs/01 要点（节） | 状态 | PLAN-ID | 备注 |
|--------------------|------|---------|------|
| 新用户首次登录全流程 &lt; 3 秒（§3.1） | `- [ ]` | **O-01** | 真机性能验收为主 |
| 到期提醒按时发送（§3.5） | `- [~]` | **O-02**、**O-14**、**Q-B1** | **已过期** ✅（**DOC-02**）；**到期前 N 天** ⏳ |
| 首次正式分析后示例入口隐藏（§3.6） | `- [x]` | **O-03** | 登录用户 `has_completed_real_analysis`；访客仍可看示例 |
| 100MB 视频 4G &lt; 60s（§4.2） | `- [ ]` | **O-04** | 真机实测 / W8 发布准备 |
| 断点续传（§4.2） | `- [ ]` | **O-05** | MVP **明确不做** |
| 离开等待页仍收到完成通知（§4.2） | `- [x]` | **O-06**、**Q-B1** | **DOC-01** 已回填；模板 ID + 用户授权须运营配置 |
| 骨骼叠加 ≥24fps（§4.3） | `- [ ]` | **O-07**、**Q-B4** | W6 |
| 「对比历史」并排报告（§4.3） | `- [x]` | **P-01** | `pages/analysis/compare` |
| 质量检测 5s 内完成（§4.3） | `- [ ]` | **O-08**、**Q-B4** | W6 真实预检 |
| 阻断性问题 100% 拦截（§4.3） | `- [ ]` | **O-09**、**Q-B4** | W6 |
| 警告性不阻断、报告注明（§4.3） | `- [~]` | **O-10**、**ENG-02**、**Q-B4** | 软警告 **`quality_warnings`** ✅（**DOC-03**）；硬预检 W6 |
| 非高尔夫识别与引导（§6.2） | `- [~]` | **P-02** | prompt 已有；定量测评待补 |
| 进步曲线历史点 / 多维折线 / 大卡（§6.3） | `- [~]` | **P-03**、**Q-B3** | 已部分：会员综合分 + 当月柱状 |
| 免费用户进步曲线引导（§6.3） | `- [~]` | **P-04** | 已部分：锁定 + CTA；抛光排期 |
| 分享 750×1334 PNG（§7.1） | `- [ ]` | **O-11**、**Q-B2** | |
| 小程序码扫码入场（§7.1） | `- [ ]` | **O-12**、**Q-B2** | |
| 自动续费开通/关闭（§8.4） | `- [ ]` | **O-13**、**Q-B5** | 委托扣费签约 |
| 到期提醒通知准时（§8.4） | `- [~]` | **O-14**、**O-02** | 与 §3.5 同主题（**DOC-02**）；**到期前**仍缺 |

### 6.5b 文档勾选同步（代码已交付、`docs/01` 仍为 `- [ ]`）· **DOC-\***

> **目的**：避免站会 backlog 虚高——源码与契约已落地、仅 `docs/01` 主行未勾的项，**必须先**列入本表再回填勾选（§7.2 纪律）。**不替代**功能开发 PLAN-ID（**O-\*** / **ENG-\***）。

| DOC-ID | 关联 PLAN | `docs/01` 位置 | 代码/契约锚点 | 同步动作 | 状态 |
|--------|-----------|----------------|---------------|----------|------|
| **DOC-01** | **O-06**、**Q-B1** | §4.2 离开等待页完成通知 | [`docs/02` §1.4.1](./02-API接口设计文档.md#141-微信小程序--分析完成订阅消息服务端非-rest) · [`analysis_tasks.py`](../backend/app/tasks/analysis_tasks.py) · [`wechat_subscribe_message.py`](../backend/app/integrations/wechat_subscribe_message.py) · [`waiting.tsx`](../client/src/pages/analysis/waiting.tsx) | 主行改 **`[x]`** | **Done** |
| **DOC-02** | **O-02**、**O-14**、**Q-B1** | §3.5 + §8.4 到期提醒 | [`docs/02` §1.4.2](./02-API接口设计文档.md#142-微信小程序--会员到期惰性降级订阅消息服务端非-rest) · `ensure_membership_valid` | 主行改 **`[~]`**（已过期 ✅ / 到期前 ⏳） | **Done** |
| **DOC-03** | **O-10**、**ENG-02** | §4.4 警告不阻断 | `quality_warnings` · [`qualityWarnings.ts`](../client/src/constants/qualityWarnings.ts) · `report.tsx` | 主行改 **`[~]`**；硬预检仍 **O-08** | **Done** |
| **DOC-04** | **Q-C1**、**par-C1～C3** | §3.4 注销四项已 `[x]`；缺**验收纪要** | `account-deletion` · `account_deletion_service` 等 | 产出纪要 PDF/MD + parallel C1–C3 勾 | **Open** |
| **DOC-05** | — | 全文巡检 | 脚注「已实现」vs 主行 `- [ ]` | 发版前扫描；新发现项开 **DOC-05** 子行或当场回填 | **纪律（持续）** |

### 6.5a `docs/01` §4.5 引擎产品力 ↔ PLAN-ID（**已勾选**；余量以脚注为准）

[`docs/01` §4.5](./01-MVP功能需求规格说明书.md#45-ai-引擎产品力对齐-docs20) 主验收行已为 **`[x]`**；下列便于站会仍引用 **PLAN-ID**，并与 **§6.3** 备注互链。

| docs/01 要点 | PLAN-ID | 交付快照 / 余量 |
|--------------|---------|-----------------|
| E0 · 观测层 | （**ENG-00**，不占 PLAN 行） | `docs/01` §4.5 脚注 |
| E1 · 分数说明 | **ENG-01** | `pages/help/score-guide` |
| E1 · 运营话术 | **ENG-03** | [`docs/09` §八](./09-运营支撑文档.md#八客服评分与异议话术eng-03) |
| Trust · 低置信（拍摄提示） | **ENG-02** | `quality_warnings` + [`qualityWarnings.ts`](../client/src/constants/qualityWarnings.ts)；**O-08**/**O-10** 仍见 **§6.5** |
| E2 · ECS | **ENG-04** | [`ecs-v1-starter-checklist`](./release-notes/ecs-v1-starter-checklist.md) ✅；样本库 / 回归门禁 ⏳ |
| E3 · 纵向叙事 | **ENG-05** | `score_change` 说明 ✅；分位 / 曲线见 **P-03** |
| E4 · 争议复盘 | **ENG-06** | [`docs/09` §8.5](./09-运营支撑文档.md#85-争议周更纪要模板eng-06) 模板 ✅；周更执行 ⏳ |

### 6.6 推荐迭代批次（A→G，不设日历）

与 §6.2 阶段标签对齐的 **默认拉货顺序**（按带宽调整；非强制里程碑）：

| 批次 | 阶段 | 建议装入的 PLAN-ID（示例） |
|------|------|----------------------------|
| **Batch-A** | A | **U-1**～**U-5**、**Q-A3**（生产核销、COS 真桶） |
| **Batch-B** | B | **O-01**；**O-02** / **O-14** 的「**到期前**」子能力（与 **Q-B1** 协同）；**O-06** 以配置与模板运营为主（**ENG-01** 已交付，无需再占位） |
| **Batch-C** | C | **Q-B2**、**O-11** / **O-12**、**Q-B5** / **O-13**、**P-03** / **Q-B3**、**P-01**；**ENG-05** 余量（分位 / 曲线）与进步曲线协同 |
| **Batch-D** | D | **Q-B4**、**O-07**～**O-10**（**ENG-02** 主链路已交付；本批聚焦 **O-08**～**O-10** 与 **ENG-04** 样本/回归/门禁） |
| **Batch-E** | E | **Q-D1**（RN） |
| **Batch-F** | F | **Q-C1**、**par-C1**～**par-C3**、**O-03**、**DOC-04**（验收纪要）；**ENG-03** / **ENG-06** 运营执行；**DOC-05** 发版前巡检 |
| **Batch-G** | G | **par-E3**、**par-T1** |

**说明**：**O-05**（断点续传）不进入开发批次；**U-6** / **Q-A1** 法务工单线单独跟，不纳入上表「功能批次」。

### 6.7 执行纪律（摘录）

1. 契约 / 字段变更先改 [`docs/02-API接口设计文档.md`](./02-API接口设计文档.md)，再改代码；数据库走 models → `make backend-revision` → `make backend-migrate`。
2. **一事一 PR**，说明见 [`docs/10-Git协作规范.md`](./10-Git协作规范.md)。
3. **占位页**（教练 / 训练等）不擅自扩功能；排期变更先改 `docs/01` / 白皮书。
4. **UI** 品牌色走 `client/src/app.scss` CSS 变量，业务 SCSS 不硬编码主色 HEX。

---

## 七、MVP 一期收尾（工程真源 · 精简）

> **本节只保留「一期必须收口」与「明确不做」**；完整 PLAN-ID、未勾验收行仍以 **§六**、`docs/01` 为准，避免把愿景/二期能力堆成当期需求。

### 7.1 明确不做（MVP）

| 项 | 说明 |
|----|------|
| **O-05 断点续传** | `docs/01` 已声明；参数表与验收条一致为 **不支持（一期）** |

### 7.2 一期必收口（真引擎可感知 + 可观测）

| 主题 | 动作 | 验证 |
|------|------|------|
| **生产误开 Mock** | 引擎默认真实管线；Compose `AI_ENGINE_MOCK_MODE=${AI_ENGINE_MOCK_MODE:-false}`；CVM `.env.local` 勿留空导致旧版默认 Mock | `GET {AI_ENGINE_URL}/health` → `mock_mode: false`；`GET /v1/health` → `services.ai_engine` 为 `ok`，且 `ai_engine.mock_mode` 为 `false` |
| **骨骼 / 衍生视频** | `real_pipeline` + MinIO/COS 衍生上传开启时，报告内骨骼资源为真实推理产物 | 走一条完整分析，核对报告字段与对象存储 key；非 Mock 下缩略图/骨骼视频应与随机 Mock 明显不同 |
| **`docs/01` 未勾验收** | 仍开放的 `- [ ]`/`- [~]` 以 **§6.5** 驱动；**代码已交付、文档未勾**必须先入 **§6.5b DOC-\*** 再回填（**DOC-01～03** 已闭环） | 站会跟 **PLAN-ID**；文档债跟 **DOC-\*** |
| **引擎产品力持续迭代** | **Trust / Calibration / Coach Consensus**、评分话术、**ECS 标定集**；`docs/01` **§4.5** 主行已勾，**余量**见 **§6.5a** 与 **§6.3** `ENG-04`/`ENG-05`/`ENG-06` 备注 | [`docs/20`](./20-AI引擎产品力迭代设计.md)；与 **Q-B4**、**O-07**～**O-10** 交叉 |

### 7.3 非当期承诺（不纳入 §二/§七 排期压力）

白皮书 §5.2 中的 **向量知识库 / 多维看板 / RN** 等按 **§三 Q-D1**、白皮书 §5.3～5.4 排期；**不**在本节重复展开为「缺口表」。

---

## 文档变更记录

| 日期 | 说明 |
|------|------|
| 2026-05-13 | 初始化：汇合 MVP / W9 / 并行 backlog 与仓库实测对齐 |
| 2026-05-18 | 新增 **§二 紧急队列（U-1～U-6）**；§一 增补「紧急队列」标签说明；§三（原 §二）矩阵与 **U-x** 交叉引用；章节顺延重编号 |
| 2026-05-18 | **Q-B1**：会员页补「到期提醒」订阅授权；waiting / membership 的 `requestSubscribeMessage` 统一补充 `entityIds: []` |
| 2026-05-18 | **Q-B3**：训练 Tab 进步曲线 MVP：会员综合分 + 本月打卡柱；免费锁定 |
| 2026-05-20 | 后端：`subscribeMessage.send` 分析完成（可配置）；`docs/02` §1.4.1；`.env.example` |
| 2026-05-18 | **§六** 增补 **§6.5** `docs/01` ↔ PLAN-ID 对照表、**§6.6** 推荐迭代批次；原执行纪律顺延为 **§6.7**；**§6.3** 刷新 **Q-B1** / **O-02** / **O-06** / **O-14** 订阅与提醒备注 |
| 2026-05-18 | **§七** 收敛为「一期收尾」：真引擎/Mock 可观测、断点续传不纳入；愿景/二期能力改指 §六，避免堆成当期需求 |
| 2026-05-18 | 新增 [`docs/20-AI引擎产品力迭代设计.md`](./20-AI引擎产品力迭代设计.md)：引擎三线、评分话术、ECS；白皮书 **§5.2.1**；**§七 §7.2** 增「引擎产品力」行；`docs/01` / `docs/05` / `AGENTS` / `W6-engine-productization-roadmap` 互链 |
| 2026-05-18 | **配套**：`docs/02` §8.4 `GET /v1/health` 增补 `ai_engine` 探针字段；`docs/01` §4.1 分片表「断点续传」与 **O-05** 对齐为不支持（一期） |
| 2026-05-18 | **ENG-01 / ENG-03 首版交付**：小程序「分数说明」帮助页；[`docs/09` §八](./09-运营支撑文档.md) 客服话术；`docs/01` §4.5 勾选更新 |
| 2026-05-18 | **`docs/01` §4.5** 引擎产品力验收行入库；**§6.3** 新增 **ENG-01～ENG-06**；**§6.5** 增补映射；**§6.6** 批次装入 ENG；**§7.2** 指向 ENG |
| 2026-05-18 | **ENG-02** `quality_warnings` 全链路 + 报告「拍摄提示」；`docs/02` §3.4/§3.4c、`docs/03` `0015` 说明；**ECS** 启动清单 [`ecs-v1-starter-checklist`](./release-notes/ecs-v1-starter-checklist.md)；**§6.3** 刷新 **ENG-01～ENG-06**；**§6.5a**（§4.5 已勾存档）；**§6.6** 与 ENG 对齐 |
| 2026-05-18 | **§6.5b DOC-01～05**：代码已交付、`docs/01` 未勾列入计划；**DOC-01～03** 已回填 **O-06** `[x]`、到期提醒 **DOC-02** `[~]`、**O-10** **DOC-03** `[~]`；**§6.1** `- [ ]` **10**、`- [~]` **7**；**§四** 增补订阅消息；**DOC-04** 仍 Open（par-C1～C3 验收纪要） |
| 2026-05-20 | **视觉规范回填**（**Round 2 一次扫齐**）：白皮书 §7.2 + AGENTS §3 + `docs/01` §2.4 + `docs/04` §1.5 + `docs/09` 上线清单 + `README` + `client/README` + PR 模板 + `EnvBadge.tsx` / `scoreLevel.ts` 注释 + `gen_tab_icons.py` 注释 + W8-tab-icons.md 一次性以 `client/src/app.scss` 为权威源回填为「靛蓝 #1a237e + 白 + 金 #c9a227 + 点缀绿 #00d084」；不动客户端 PNG 二进制（W8 占位激活态仍是 #0f3d2e，在 W8-tab-icons.md 标注为已知 drift，待 W9 设计师正式出图替换） |
