# P2-M13-09 · 服务协议 + 首次强提醒 + 未成年保护 + 女性安全 · 启动包（W28 起跑）

> 版本：v0.1（2026-05-25）
> 上游真源：[`docs/23 §9.9`](../23-二期可编码规格说明书.md#99-p2-m13-09--服务协议更新--首次使用强提醒--未成年保护--女性安全)
> 前置：DEP-05 法务

---

## 一、文档目的与边界

为 **P2-M13-09** 落地 W28-W32 产品 + 法务 + 客户端 SOP，确保 M13 上线前合规四大要素到位。

### 边界（不做）

- 不修改 docs/22/23/06 字段
- 不允许 14 岁以下用户绕开

---

## 二、现状盘点

- 一期服务协议无 M13 增量条款
- 一期 user 表无 age 字段（依赖实名 / DEP）
- 一期无强弹窗 / 同意流程

### 缺口

6 个 FR 全部新增。

---

## 三、模块设计

### 3.1 改造 + 新增

| 模块 | 路径 | 工程量 |
| --- | --- | --- |
| 服务协议增量 | `docs/legal/tos-m13.md`（新） + 客户端 | 0.5 PW |
| 首次强弹窗 | `components/MeetupTosModal.tsx`（新） | 0.5 PW |
| 14 岁以下拦截 | 服务层 校验 + 40332 | 0.3 PW |
| 女性安全策略 | `privacy_payload.gender_preference` + UI | 0.5 PW |
| 法务签字 | DEP-05 | — |
| 单测 | tests | 0.2 PW |

**合计：~2 PW**

### 3.2 14 岁以下校验

```python
def check_meetup_eligibility(user):
    if user.age and user.age < 14:
        raise ForbiddenError(40332, '14 岁以下不开放约球功能')
    if not user.is_phone_verified:
        raise ForbiddenError(40333, '请先完成手机号实名')
```

### 3.3 女性安全选项

```python
gender_preference: Literal['any', 'same', 'coach_only']
```

UI：
```
[匹配偏好]
  ◉ 任意匹配
  ○ 仅匹配同性别（女性默认）
  ○ 仅匹配教练
```

至少 1 位女性产品顾问参与设计 review（DEP-05 列入）。

### 3.4 协议弹窗

```
平台只做信息匹配，**不参与任何线下活动**，**不承担任何责任**。
线下活动请确保人身/财产安全。
[同意] [拒绝]
```

拒绝 → M13 整体不可用。

---

## 四、字段 v0.1

```python
# user_profiles_v2.privacy_payload
gender_preference: 'any' | 'same' | 'coach_only'
meetup_tos_accepted_at: timestamptz
```

错误码：40332 / 40333

---

## 五、验证数据

- 协议覆盖率 100%（AC-1）
- 14 岁以下拦截（AC-2）
- 女性策略 + 顾问签字（AC-3）
- DEP-05 完成（AC-4）

---

## 六、W28-W32 周计划

| 周 | 任务 |
| --- | --- |
| W28 | 协议文本 + 法务 review |
| W29 | 弹窗 + 校验 |
| W30 | 女性选项 + UI |
| W31 | 单测 + 女性顾问 review |
| W32 | 灰度 + AC |

---

## 七、责任 / 风险 / 验收

### 责任

| 角色 | 责任 |
| --- | --- |
| 法务 | DEP-05 + 协议 |
| 产品 | 女性顾问对接 |
| 客户端 | 弹窗 + 设置 |
| 后端 | 14 岁校验 |

### 风险

| ID | 风险 | 兜底 |
| --- | --- | --- |
| R-01 | DEP-05 延期 | 协议先用 v0.1；上线前必到 |
| R-02 | 用户跳过弹窗 | 强弹 + 不可关闭 |
| R-03 | 年龄字段缺失 | 实名走通后才开放 M13 |
| R-04 | 女性安全策略不完整 | 多轮 review |

### AC

- [ ] AC-1 协议 100%
- [ ] AC-2 14 岁以下拦截
- [ ] AC-3 女性策略 + 签字
- [ ] AC-4 DEP-05

---

## 八、附录

| 任务 | 关系 |
| --- | --- |
| P2-M13-05 隐私 | gender_preference |
| 一期实名 | 14 岁校验源 |
| DEP-05 法务 | 红线 |

---

## 九、变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v0.1 | 2026-05-25 | 初版 |
