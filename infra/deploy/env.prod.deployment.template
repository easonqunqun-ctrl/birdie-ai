# 复制到 Mac 密钥真源：`cp infra/deploy/env.prod.deployment.template ~/secrets/lingniao-prod.env`
# 填真值后：`scp ~/secrets/lingniao-prod.env ubuntu@CVM:$DEPLOY_REPO/.env.local`
# 勿提交含真实密钥的文件到 Git。

APP_ENV=staging
APP_DEBUG=false
APP_NAME=xiaoniao-ai
APP_SECRET_KEY=REPLACE_WITH_RANDOM_48
APP_LOG_LEVEL=INFO

BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_WORKERS=2
API_PUBLIC_BASE_URL=https://api.birdieai.cn
BACKEND_CORS_ORIGINS=https://api.birdieai.cn,https://servicewechat.com

JWT_SECRET_KEY=REPLACE_WITH_RANDOM_48
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_DAYS=7

POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=xiaoniao
POSTGRES_PASSWORD=REPLACE_DB_PASSWORD
POSTGRES_DB=xiaoniao
DATABASE_URL=postgresql+asyncpg://xiaoniao:REPLACE_DB_PASSWORD@postgres:5432/xiaoniao
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://redis:6379/0

STORAGE_PROVIDER=minio
MINIO_ENDPOINT=http://minio:9000
MINIO_PUBLIC_ENDPOINT=https://api.birdieai.cn/minio
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=xiaoniao-videos-test
MINIO_REGION=us-east-1

WECHAT_MINIPROGRAM_APPID=REPLACE_WX_APPID
WECHAT_MINIPROGRAM_SECRET=REPLACE_WX_SECRET
WECHAT_MOCK_LOGIN=false

WECHAT_OPEN_APPID=
WECHAT_OPEN_SECRET=

# ---------- 微信支付 V3（JSAPI）— 取值来自「pay.weixin.qq.com」商户平台，勿臆造 ----------
# 接真付：WECHAT_PAY_MOCK_MODE=false；留 true 则后端走 mock（与公众平台无资金往来）。
WECHAT_PAY_MOCK_MODE=true
# 商户号「账户中心」顶部所见 10 位数字
WECHAT_PAY_MCH_ID=
# API 安全 → 设置 APIv3 密钥：须正好 32 字符（可与 WECHAT_PAY_API_KEY 二选一）
WECHAT_PAY_API_V3_KEY=
WECHAT_PAY_API_KEY=
# 二选一载入商户 API 私钥（apiclient_key.pem， PKCS#8）：
# A) WECHAT_PAY_CERT_PATH=/容器内可读路径.pem（Compose 常见做法：挂载只读密钥卷到该路径）
# B) WECHAT_PAY_PRIVATE_KEY_PEM=\"-----BEGIN PRIVATE KEY----- ... -----END PRIVATE KEY-----\"（多行；或挂载文件走 CERT_PATH）
WECHAT_PAY_CERT_PATH=
WECHAT_PAY_PRIVATE_KEY_PEM=
# 商户 API 证书 → 序列号（serial_no）；不是平台证书
WECHAT_PAY_MCH_SERIAL=
# HTTPS 公网直达本服务；须在商户平台「产品中心」等与小程序 AppID / 域名配置一致以便验签投递
WECHAT_PAY_NOTIFY_URL=https://api.birdieai.cn/v1/payments/wechat/notify
# 退款结果异步通知；留空则自动将路径 wechat/notify → wechat/refund-notify
WECHAT_PAY_REFUND_NOTIFY_URL=
# 支付成功后该小时内允许自助全额退款（小时；0 关闭窗仅测试）
PAYMENT_SELF_REFUND_WINDOW_HOURS=24

# ---------- 微信小程序虚拟支付（iOS 合规；docs/release-notes/wechat-xpay-runbook.md） ----------
# 接真付且走虚拟支付：WECHAT_PAY_MOCK_MODE=false + WECHAT_XPAY_ENABLED=true
WECHAT_XPAY_ENABLED=false
WECHAT_XPAY_OFFER_ID=
WECHAT_XPAY_APP_KEY=
WECHAT_XPAY_SANDBOX_APP_KEY=
WECHAT_XPAY_ENV=0
WECHAT_XPAY_PRODUCT_MONTHLY=
WECHAT_XPAY_PRODUCT_YEARLY=
WECHAT_MP_PUSH_TOKEN=
# WECHAT_MP_PUSH_ENCODING_AES_KEY=

APPLE_BUNDLE_ID=com.xiaoniaoai.app
APPLE_SHARED_SECRET=
APPLE_VERIFY_RECEIPT_URL=https://sandbox.itunes.apple.com/verifyReceipt

LLM_PROVIDER=deepseek
LLM_API_KEY=REPLACE_LLM_KEY
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000

AI_ENGINE_URL=http://ai_engine:9000
AI_ENGINE_TIMEOUT=60
AI_ENGINE_MOCK_MODE=false

# ---------- Phase 2 灰度（与 client/.env.production TARO_APP_PHASE2_* 双端同步；W18-A） ----------
PHASE2_PROFILE_V2_ENABLED=true
PHASE2_COURSES_ENABLED=true
PHASE2_PROS_ENABLED=true
PHASE2_MEETUP_ENABLED=true
PHASE2_COACH_ENABLED=true
PHASE2_COACH_ANNOTATIONS_ENABLED=true
PHASE2_COACH_TASKS_ENABLED=true
PHASE2_COACH_DASHBOARD_ENABLED=true
PHASE2_COACH_RECAP_ENABLED=true
# 内容审核可先 false，批注/作业走 fail-safe 自动 approved
PHASE2_COACH_CONTENT_MODERATION_ENABLED=false
PHASE2_PUTTING_MODE_ENABLED=true
PHASE2_CHIPPING_MODE_ENABLED=true
PHASE2_YARDAGE_BOOK_ENABLED=true
PHASE2_TRAINING_CATEGORIES_ENABLED=true
# 教练定制课写端点：M8 active 教练可写；seed 教练仍可用 COACH_COURSE_USER_IDS 兜底
COACH_COURSE_USER_IDS=
# 约球生产须 false（mock 登录自动成年实名仅 CI/本地）
MEETUP_MOCK_IDENTITY_VERIFIED=false

QUOTA_MODE=unlimited

COS_SECRET_ID=
COS_SECRET_KEY=
COS_REGION=ap-shanghai
COS_BUCKET=
# COS_PUBLIC_BASE=

# 监控栈（docker compose --profile monitoring）
# 企业微信群机器人 key；空则 wechat-webhook-bridge dry-run
WECOM_WEBHOOK_KEY=
